---
phase: 02-application-tracker-document-matrix
plan: 06
type: execute
wave: 4
depends_on: ["02-03", "02-04", "02-05"]
files_modified: []
autonomous: false
requirements:
  - TRACK-01
  - TRACK-02
  - TRACK-03
  - TRACK-04
  - TRACK-05
  - TRACK-06
  - TRACK-07
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-04

must_haves:
  truths:
    - "Full tracker flow works end-to-end: add from detail page, view on Kanban, drag between stages, edit notes, check documents, export CSV"
    - "Mobile flow works: tap card, use stage selector, check documents"
    - "Data persists across page refresh"
    - "Document matrix updates when document checks change"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete application tracker and document matrix flow end-to-end.

Purpose: Final human verification that all Phase 02 requirements work together as an integrated feature. This is the last gate before the phase is marked complete.
Output: User confirmation that the tracker flow works correctly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@web/.planning/phases/02-application-tracker-document-matrix/02-UI-SPEC.md
@web/.planning/phases/02-application-tracker-document-matrix/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated Smoke Tests</name>
  <files></files>
  <read_first>
    src/lib/tracker/tracker-store.ts,
    src/lib/tracker/tracker-engine.ts,
    src/lib/tracker/tracker-csv.ts,
    src/routes/tracker.tsx,
    src/components/tracker/TrackerKanban.tsx
  </read_first>
  <action>
    Run the full automated verification suite:

    1. Run all tracker unit tests:
       ```bash
       cd web && npx vitest run src/lib/tracker/ --reporter=verbose
       ```

    2. Run the full project test suite for regressions:
       ```bash
       cd web && npx vitest run
       ```

    3. Run TypeScript type checking:
       ```bash
       cd web && npx tsc --noEmit
       ```

    4. Verify all 12 tracker components exist:
       ```bash
       ls src/components/tracker/
       ```
       Expected: DocumentChecklist.tsx, DocumentMatrix.tsx, ExportCSVButton.tsx, MobileStageSelector.tsx, NotesEditor.tsx, StageBadge.tsx, TrackThisButton.tsx, TrackerCard.tsx, TrackerCardExpanded.tsx, TrackerColumn.tsx, TrackerEmptyState.tsx, TrackerKanban.tsx

    5. Verify route registration:
       ```bash
       grep "createFileRoute.*tracker" src/routes/tracker.tsx
       ```

    6. Verify Navbar integration:
       ```bash
       grep "/tracker" src/components/layout/Navbar.tsx
       ```

    7. Verify Convex schema extension:
       ```bash
       grep "document_requirements" convex/schema.ts
       ```

    8. Start the dev server to confirm build succeeds:
       ```bash
       cd web && npx vinxi build 2>&1 | tail -20
       ```
       Build should complete without errors.

    If any step fails, fix the issue before proceeding to the human verification checkpoint.
  </action>
  <verify>
    <automated>cd /home/siam/Personal/ScholarHub/web && npx vitest run --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/tracker/` exits 0 with all tests passing
    - `npx vitest run` exits 0 with no regressions
    - `npx tsc --noEmit` exits 0
    - All 12 tracker component files exist in src/components/tracker/
    - `/tracker` route file exists
    - Navbar has "/tracker" link
    - Convex schema has document_requirements field
    - Build completes without errors
  </acceptance_criteria>
  <done>All automated checks pass: unit tests green, TypeScript clean, build succeeds, all files exist.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human Verification of Full Tracker Flow</name>
  <files></files>
  <action>
    Present the following verification steps to the user and wait for approval.

    Start the dev server: `cd web && npm run dev`

    **Test 1: Add to Tracker (TRACK-01)**
    1. Visit any scholarship detail page (e.g., /scholarships/chevening-scholarship)
    2. Verify "Track This" button appears after "Apply Now"
    3. Click "Track This" -- button should change to "Tracking", toast says "Added to tracker"
    4. Scroll down -- StickyBar should show a tracker icon button
    5. Go back to /scholarships directory -- the card for the tracked scholarship should show "Tracking" in footer

    **Test 2: Kanban Board (TRACK-02, TRACK-03, TRACK-04)**
    6. Click "Tracker" in the navbar to visit /tracker
    7. Verify Kanban board with 5 columns: Researching, Preparing, Submitted, Interview, Result
    8. Verify the tracked scholarship appears in "Researching" column
    9. Verify card shows: title, deadline badge, document status (0/6)
    10. On desktop: drag the card from Researching to Preparing -- toast says "Moved to Preparing"
    11. On mobile (resize browser): tap the card, use stage selector pills to move

    **Test 3: Expanded Card (TRACK-05, DOC-01, DOC-02)**
    12. Click a tracker card to expand it
    13. Verify expanded view shows: stage selector, document checklist, notes editor
    14. Check 2-3 documents -- checkboxes should toggle immediately
    15. Type notes (verify 500 char limit shows, debounced save)
    16. Close expanded view (click outside or Escape key)
    17. Card should now show updated document count (e.g., "2/6")

    **Test 4: Document Matrix (DOC-03)**
    18. Add 2-3 more scholarships to tracker from detail pages
    19. On /tracker, scroll below Kanban to "Document Overview" section
    20. Verify grid shows documents vs scholarships with check states
    21. Check off documents for different scholarships
    22. Verify matrix updates in real-time

    **Test 5: CSV Export (TRACK-07)**
    23. Click "Export CSV" button next to page title
    24. Verify CSV file downloads with name pattern: scholarhub-applications-YYYY-MM-DD.csv
    25. Open CSV -- verify columns: Title, Stage, Deadline, Notes, Documents Completed, Date Added

    **Test 6: Persistence (TRACK-06)**
    26. Refresh the page
    27. Verify all tracked scholarships, stage positions, notes, and document checks persist
    28. Open a second tab to /tracker -- verify same data appears (cross-tab sync)

    **Test 7: Edge Cases**
    29. Verify empty state shows when no items tracked (clear all or fresh browser)
    30. Remove a scholarship from the expanded card -- verify it disappears from board
    31. Verify "Track This" button reverts to original state for removed scholarship
  </action>
  <verify>User responds with "approved" or describes issues to fix</verify>
  <acceptance_criteria>
    - User confirms all 7 test groups pass
    - No critical bugs reported
  </acceptance_criteria>
  <done>User approves the complete tracker feature. Phase 02 requirements verified end-to-end.</done>
</task>

</tasks>

<verification>
All automated tests pass and human verifies the complete flow works end-to-end.
</verification>

<success_criteria>
- All 11 requirements (TRACK-01 through TRACK-07, DOC-01 through DOC-04) verified working
- Drag-and-drop functional on desktop
- Tap-to-move functional on mobile
- Notes persist with debounced auto-save
- Document checks persist immediately
- Document matrix aggregates correctly
- CSV exports with correct format
- Data persists across page refresh
- Cross-tab sync works
- Empty state renders correctly
- User approves the feature
</success_criteria>

<output>
After completion, create `.planning/phases/02-application-tracker-document-matrix/02-06-SUMMARY.md`
</output>
