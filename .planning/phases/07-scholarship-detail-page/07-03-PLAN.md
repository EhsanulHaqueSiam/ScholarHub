---
phase: 07-scholarship-detail-page
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - web/src/routes/scholarships/$slug.tsx
autonomous: false
requirements:
  - DTLP-01
  - DTLP-02
  - DTLP-03
  - DTLP-04
  - DTLP-05
  - DTLP-06
  - DTLP-07
  - DTLP-08
  - DTLP-09
  - DTLP-10
  - DTLP-11

must_haves:
  truths:
    - "Detail page loads scholarship data via getScholarshipDetail query with resolved sources"
    - "All 6 section cards render in correct order: Hero, Overview, Eligibility, Funding, How to Apply, Sources"
    - "Sticky bar appears after scrolling past hero with truncated title, Copy Link, Apply Now"
    - "Breadcrumb preserves filter context from directory page via URL search params"
    - "Schema.org JSON-LD includes eligibility, degree levels, fields, funding, deadline, source attribution"
    - "Meta title follows format: {Title} -- {Funding Type} {Degree} Scholarship in {Country} | ScholarHub"
    - "Loading state shows DetailSkeleton, not-found state shows 404 page with browse link"
    - "User can visually verify the complete detail page works end-to-end"
  artifacts:
    - path: "web/src/routes/scholarships/$slug.tsx"
      provides: "Complete scholarship detail page with all sections, SEO, breadcrumb, sticky bar"
      min_lines: 150
  key_links:
    - from: "web/src/routes/scholarships/$slug.tsx"
      to: "web/convex/directory.ts"
      via: "useQuery(api.directory.getScholarshipDetail, { slug })"
      pattern: "useQuery.*getScholarshipDetail"
    - from: "web/src/routes/scholarships/$slug.tsx"
      to: "web/src/components/detail/HeroSection.tsx"
      via: "import HeroSection"
      pattern: "import.*HeroSection"
    - from: "web/src/routes/scholarships/$slug.tsx"
      to: "web/src/components/detail/StickyBar.tsx"
      via: "import StickyBar"
      pattern: "import.*StickyBar"
    - from: "web/src/routes/scholarships/$slug.tsx"
      to: "web/src/components/detail/SourcesSection.tsx"
      via: "import SourcesSection with resolved_sources prop"
      pattern: "import.*SourcesSection"
---

<objective>
Rewrite the scholarship detail page route ($slug.tsx) to wire all section components from Plan 02, integrate the Convex query from Plan 01, expand Schema.org JSON-LD, add meta tags, breadcrumb with filter state, and sticky bar scroll behavior. Then verify the full page visually.

Purpose: This is the integration layer that connects all 10 section components into a complete, functional detail page. It replaces the Phase 06.1 placeholder entirely.

Output: Complete rewrite of $slug.tsx as the final detail page, followed by human visual verification.
</objective>

<execution_context>
@/home/siam/.claude/get-shit-done/workflows/execute-plan.md
@/home/siam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-scholarship-detail-page/07-CONTEXT.md
@.planning/phases/07-scholarship-detail-page/07-RESEARCH.md
@.planning/phases/07-scholarship-detail-page/07-UI-SPEC.md
@.planning/phases/07-scholarship-detail-page/07-01-SUMMARY.md
@.planning/phases/07-scholarship-detail-page/07-02-SUMMARY.md

<interfaces>
<!-- All section components from Plan 02 -->

From web/src/components/detail/HeroSection.tsx:
```typescript
// forwardRef component
interface HeroSectionProps {
  title: string;
  providerOrganization: string;
  prestigeTier: PrestigeTier;
  hostCountry: string;
  applicationDeadline?: number;
  degreeLevels: string[];
  fundingType: string;
  applicationUrl?: string;
}
```

From web/src/components/detail/StickyBar.tsx:
```typescript
interface StickyBarProps {
  title: string;
  slug: string;
  applicationUrl?: string;
  visible: boolean;
  isExpired: boolean;
}
```

From web/src/components/detail/Breadcrumb.tsx:
```typescript
interface DetailBreadcrumbProps {
  scholarshipTitle: string;
  searchParams?: Record<string, string | undefined>;
}
```

From web/src/components/detail/OverviewSection.tsx:
```typescript
interface OverviewSectionProps { description: string | null | undefined; }
```

From web/src/components/detail/EligibilitySection.tsx:
```typescript
interface EligibilitySectionProps {
  nationalities: string[] | null | undefined;
  degreeLevels: string[];
  fieldsOfStudy: string[] | null | undefined;
}
```

From web/src/components/detail/FundingSection.tsx:
```typescript
interface FundingSectionProps {
  fundingType: string;
  fundingTuition: boolean | undefined;
  fundingLiving: boolean | undefined;
  fundingTravel: boolean | undefined;
  fundingInsurance: boolean | undefined;
  awardAmountMin: number | undefined;
  awardAmountMax: number | undefined;
  awardCurrency: string | undefined;
}
```

From web/src/components/detail/HowToApplySection.tsx:
```typescript
interface HowToApplySectionProps {
  applicationDeadline?: number;
  applicationUrl?: string;
  editorialNotes?: string | null;
  expectedReopenMonth?: number;
}
```

From web/src/components/detail/SourcesSection.tsx:
```typescript
interface SourcesSectionProps {
  resolvedSources: Array<{ name: string; url: string }>;
  lastVerified: number | undefined;
  sourceCount: number;
}
```

From web/src/components/detail/DetailSkeleton.tsx:
```typescript
export function DetailSkeleton(): JSX.Element;
```

From web/src/lib/deadline.ts:
```typescript
export function useIsHeroVisible(heroRef: React.RefObject<HTMLElement | null>): boolean;
```

From web/convex/directory.ts (getScholarshipDetail return type):
```typescript
// Returns scholarship doc with additional field:
resolved_sources: Array<{ name: string; url: string }>
```

From web/src/lib/filters.ts (search params schema):
```typescript
export const scholarshipSearchSchema = z.object({
  q: z.string().optional(),
  from: z.string().optional(),
  to: z.string().optional(),
  degree: z.string().optional(),
  field: z.string().optional(),
  funding: z.string().optional(),
  tier: z.string().optional(),
  sort: z.enum(["deadline", "prestige", "newest", "amount"]).optional(),
  // ... more fields
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite $slug.tsx -- wire all sections, expanded JSON-LD, meta tags, sticky bar, breadcrumb</name>
  <files>web/src/routes/scholarships/$slug.tsx</files>
  <read_first>
    - web/src/routes/scholarships/$slug.tsx (current placeholder to fully rewrite)
    - web/src/components/detail/HeroSection.tsx (props interface to match)
    - web/src/components/detail/StickyBar.tsx (props interface to match)
    - web/src/components/detail/Breadcrumb.tsx (props interface to match)
    - web/src/components/detail/OverviewSection.tsx (props interface to match)
    - web/src/components/detail/EligibilitySection.tsx (props interface to match)
    - web/src/components/detail/FundingSection.tsx (props interface to match)
    - web/src/components/detail/HowToApplySection.tsx (props interface to match)
    - web/src/components/detail/SourcesSection.tsx (props interface to match)
    - web/src/components/detail/DetailSkeleton.tsx (loading state component)
    - web/src/lib/deadline.ts (useIsHeroVisible hook)
    - web/src/lib/shared.ts (formatFundingType for meta title)
    - web/src/lib/countries.ts (getCountryName for meta title and JSON-LD)
    - web/src/lib/prestige.ts (PrestigeTier type)
    - web/src/lib/filters.ts (getDeadlineUrgency for expired check, scholarshipSearchSchema)
    - web/convex/directory.ts (getScholarshipDetail query)
    - web/src/components/layout/Navbar.tsx (shared navbar)
    - web/src/components/layout/BackToTop.tsx (scroll-to-top button)
    - .planning/phases/07-scholarship-detail-page/07-UI-SPEC.md (page structure, SEO contract, layout contract)
    - .planning/phases/07-scholarship-detail-page/07-CONTEXT.md (meta title format, JSON-LD expansion, breadcrumb decisions)
  </read_first>
  <action>
Completely rewrite `web/src/routes/scholarships/$slug.tsx`. Remove ALL existing code and replace with the following structure:

**Imports:**
- `createFileRoute, Link` from `@tanstack/react-router`
- `useQuery` from `convex/react`
- `useRef` from `react`
- `z` from `zod`
- `api` from `../../../convex/_generated/api` (typed -- NOT `anyApi` from `convex/server`)
- `Navbar` from `@/components/layout/Navbar`
- `BackToTop` from `@/components/layout/BackToTop`
- All 10 detail components: `HeroSection`, `StickyBar`, `DetailBreadcrumb`, `OverviewSection`, `EligibilitySection`, `FundingSection`, `HowToApplySection`, `SourcesSection`, `DetailSkeleton`
- `useIsHeroVisible` from `@/lib/deadline`
- `formatFundingType` from `@/lib/shared`
- `getCountryName` from `@/lib/countries`
- `getDeadlineUrgency` from `@/lib/filters`
- `PrestigeTier` type from `@/lib/prestige`

**Search params validation (for breadcrumb filter context):**
Define `detailSearchSchema` as a Zod object with optional string fields: `from`, `to`, `degree`, `field`, `funding`, `tier`, `sort`. These carry the referrer filter params so the breadcrumb can link back to a filtered directory.

**Route definition:**
- `validateSearch: (search) => detailSearchSchema.parse(search)`
- `head()`: Use slug-based title as fallback: `${params.slug.replace(/-/g, " ").replace(/\b\w/g, (c) => c.toUpperCase())} | ScholarHub`. Description: generic fallback referencing the slug.
- `component: ScholarshipDetailPage`

**buildScholarshipJsonLd function (expanded from existing):**
Keep existing fields (name, provider, url, description, applicationDeadline, eligibleRegion, studyLocation, amount) and ADD these new fields per 07-UI-SPEC SEO contract:
- `funder`: `{ "@type": "Organization", name: provider_organization }`
- `educationalLevel`: `degree_levels` array (e.g., `["PhD", "Masters"]`) -- only if non-empty
- `occupationalCategory`: `fields_of_study` array -- only if non-empty
- `dateModified`: `new Date(last_verified).toISOString()` -- only if last_verified exists
- The `url` field: guard `window.location.origin` with `typeof window !== "undefined"` for SSR safety (same pattern as existing)

NOTE: The JSON-LD is rendered via a script tag with `type="application/ld+json"`. The data source is the Convex database (trusted, admin-curated content) -- this is the same safe pattern already used in the existing placeholder.

**buildMetaTitle function:**
Format: `{Title} -- {Funding Type} {Degree} Scholarship in {Country} | ScholarHub`
- Use `formatFundingType(scholarship.funding_type)` for funding label
- Use first degree level, capitalized, for degree
- Use `getCountryName(scholarship.host_country)` for country
- Fallback (missing degree or country): `{Title} | ScholarHub`

**ScholarshipDetailPage component:**
1. Get slug from `Route.useParams()`, search from `Route.useSearch()`
2. Create `heroRef = useRef<HTMLDivElement>(null)` and `isHeroVisible = useIsHeroVisible(heroRef)`
3. Query: `useQuery(api.directory.getScholarshipDetail, { slug })`
4. **Loading state** (scholarship === undefined): Render `<Navbar />` + `<DetailSkeleton />` inside the page container
5. **Not found state** (scholarship === null): Render "Scholarship Not Found" heading + "The scholarship you're looking for doesn't exist or has been removed." + "Browse All Scholarships" Link button to `/scholarships`
6. **Loaded state**: Derive `prestigeTier`, `isExpired` (from getDeadlineUrgency), `scholarshipSlug` (slug or _id), JSON-LD string. Update `document.title` with `buildMetaTitle` (client-side only, guard with `typeof document !== "undefined"`).

Render in order:
- `<Navbar />`
- `<StickyBar>` (visible when `!isHeroVisible`, passes title, slug, applicationUrl, isExpired)
- JSON-LD script tag
- Container div: `pt-20 pb-16 px-4 md:px-6` > `max-w-3xl mx-auto space-y-8`
  - `<DetailBreadcrumb>` with scholarshipTitle and searchParams from route search
  - `<HeroSection ref={heroRef}>` with all hero props mapped from scholarship fields (use `?? undefined` for optional number fields)
  - `<OverviewSection>` with description
  - `<EligibilitySection>` with nationalities, degreeLevels, fieldsOfStudy
  - `<FundingSection>` with fundingType, all 4 coverage booleans, amount fields
  - `<HowToApplySection>` with deadline, applicationUrl, editorialNotes, expectedReopenMonth
  - `<SourcesSection>` with resolved_sources, last_verified, source_ids.length as sourceCount
- `<BackToTop />`

Section order matches the locked decision from CONTEXT.md: Hero -> Overview -> Eligibility -> Funding -> How to Apply -> Sources.
  </action>
  <verify>
    <automated>cd /home/siam/Personal/ScholarHub/web && npx tsc --noEmit 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - web/src/routes/scholarships/$slug.tsx does NOT contain `anyApi` (replaced with typed api)
    - web/src/routes/scholarships/$slug.tsx contains `api.directory.getScholarshipDetail` (typed query)
    - web/src/routes/scholarships/$slug.tsx contains `useIsHeroVisible` (sticky bar hook)
    - web/src/routes/scholarships/$slug.tsx contains `<HeroSection` (hero section rendered)
    - web/src/routes/scholarships/$slug.tsx contains `<StickyBar` (sticky bar rendered)
    - web/src/routes/scholarships/$slug.tsx contains `<DetailBreadcrumb` (breadcrumb rendered)
    - web/src/routes/scholarships/$slug.tsx contains `<OverviewSection` (overview rendered)
    - web/src/routes/scholarships/$slug.tsx contains `<EligibilitySection` (eligibility rendered)
    - web/src/routes/scholarships/$slug.tsx contains `<FundingSection` (funding rendered)
    - web/src/routes/scholarships/$slug.tsx contains `<HowToApplySection` (how to apply rendered)
    - web/src/routes/scholarships/$slug.tsx contains `<SourcesSection` (sources rendered)
    - web/src/routes/scholarships/$slug.tsx contains `<DetailSkeleton` (loading state)
    - web/src/routes/scholarships/$slug.tsx contains `Scholarship Not Found` (404 state)
    - web/src/routes/scholarships/$slug.tsx contains `Browse All Scholarships` (404 CTA)
    - web/src/routes/scholarships/$slug.tsx contains `educationalLevel` (expanded JSON-LD)
    - web/src/routes/scholarships/$slug.tsx contains `occupationalCategory` (expanded JSON-LD)
    - web/src/routes/scholarships/$slug.tsx contains `dateModified` (expanded JSON-LD)
    - web/src/routes/scholarships/$slug.tsx contains `funder` (expanded JSON-LD)
    - web/src/routes/scholarships/$slug.tsx contains `validateSearch` (search params for breadcrumb)
    - web/src/routes/scholarships/$slug.tsx does NOT contain `Full detail page coming in Phase 7` (placeholder removed)
    - `npx tsc --noEmit` produces no errors
  </acceptance_criteria>
  <done>$slug.tsx is completely rewritten with all 10 section components wired, expanded JSON-LD with educationalLevel/occupationalCategory/dateModified/funder, structured meta title, breadcrumb with filter context, sticky bar with IntersectionObserver, loading skeleton, and 404 state. No placeholder text remains.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete detail page</name>
  <files>web/src/routes/scholarships/$slug.tsx</files>
  <action>
Human visually verifies the complete scholarship detail page. The executor starts the dev server and presents verification instructions to the user. No code changes in this task -- purely verification of Task 1 output.

What was built: Complete scholarship detail page replacing the Phase 06.1 placeholder with breadcrumb navigation, hero section (prestige-tinted card, title, provider, badges, deadline urgency, Apply Now), sticky top bar on scroll, overview section, eligibility section (nationality flags with expand/collapse, degree/field badges), funding section (checklist with Check/X/Minus icons, award amount), How to Apply section (deadline countdown, timezone display, Apply button, ScholarHub Tips callout), sources section (clickable source links, trust signal, last verified, stale warning), expanded Schema.org JSON-LD, and loading/404 states.

Verification steps for the user:
1. Start dev server: `cd web && npm run dev`
2. Navigate to a scholarship detail page from the /scholarships directory
3. Check hero section: title, provider, prestige badge, country flag, deadline urgency, Apply Now
4. Scroll down -- sticky bar should appear with truncated title, Copy Link, Apply Now
5. Click Copy Link -- should show "Link copied!" briefly
6. Check all sections: Overview, Eligibility (expand nationalities), Funding (checklist icons), How to Apply (deadline, tips), Sources (links, verified date)
7. Check breadcrumb: "Scholarships > {Title}" -- click Scholarships to return to directory
8. Toggle dark mode -- all sections should adapt
9. Resize to mobile -- title shrinks, Apply goes full width, breadcrumb truncates
10. Navigate to /scholarships/nonexistent-slug -- should show 404 with "Browse All Scholarships" link
  </action>
  <verify>User types "approved" or describes issues to fix.</verify>
  <done>Human has visually verified the complete detail page works correctly across all sections, states (loading, loaded, 404), dark mode, mobile, and interactive behaviors (sticky bar, copy link, nationality expand).</done>
</task>

</tasks>

<verification>
TypeScript compilation for Task 1, then full visual verification for Task 2. The human checkpoint ensures the integrated page works end-to-end before the phase is marked complete.
</verification>

<success_criteria>
- $slug.tsx is fully rewritten with no placeholder content
- All 10 section components render correctly in the route
- Schema.org JSON-LD includes educationalLevel, occupationalCategory, dateModified, funder
- Meta title follows "{Title} -- {Funding Type} {Degree} Scholarship in {Country} | ScholarHub" format
- Sticky bar appears/hides based on scroll position
- Breadcrumb preserves filter context
- Human approves visual verification
</success_criteria>

<output>
After completion, create `.planning/phases/07-scholarship-detail-page/07-03-SUMMARY.md`
</output>
