---
phase: 06.1-country-eligibility-filtering-university-tier-list-prestige-highlighting
plan: 05
type: execute
wave: 3
depends_on: ["06.1-01", "06.1-03", "06.1-04"]
files_modified:
  - web/src/components/directory/SearchBar.tsx
  - web/src/components/directory/EligibilityFilterBar.tsx
  - web/src/components/directory/CountrySelector.tsx
  - web/src/components/directory/NationalityBanner.tsx
  - web/src/components/directory/FilterPanel.tsx
  - web/src/components/directory/FilterChips.tsx
  - web/src/components/directory/QuickFilters.tsx
  - web/src/components/directory/SortPills.tsx
  - web/src/components/directory/ViewToggle.tsx
autonomous: true
requirements:
  - PDIR-01
  - PDIR-02
  - PDIR-03
  - PDIR-04
  - PDIR-05
  - PDIR-06

must_haves:
  truths:
    - "User can type in search bar and see suggestion dropdown with top 5 mixed results"
    - "User can set nationality and destination countries via the smart eligibility filter bar"
    - "Eligibility bar compacts to single line after nationality and destinations are set"
    - "User can filter by degree level, field of study, funding type, and prestige tier"
    - "Active filters show as removable chips above results with 'Clear all' option"
    - "Quick filter tabs for 'Open Now', 'Closing Soon', 'Fully Funded', 'Gold Tier' work with AND logic"
    - "Sort pills switch between Deadline, Prestige, Newest, Amount"
    - "View toggle switches between grid (3-col) and list view"
  artifacts:
    - path: "web/src/components/directory/SearchBar.tsx"
      provides: "Search input with Radix Popover suggestion dropdown"
      exports: ["SearchBar"]
    - path: "web/src/components/directory/EligibilityFilterBar.tsx"
      provides: "Smart nationality + destination filter bar with compact mode"
      exports: ["EligibilityFilterBar"]
    - path: "web/src/components/directory/FilterPanel.tsx"
      provides: "Left sidebar (desktop) / bottom sheet (mobile) with all filter controls"
      exports: ["FilterPanel"]
    - path: "web/src/components/directory/FilterChips.tsx"
      provides: "Active filter pills with remove and clear all"
      exports: ["FilterChips"]
    - path: "web/src/components/directory/QuickFilters.tsx"
      provides: "Toggle chip row for quick filter tabs"
      exports: ["QuickFilters"]
    - path: "web/src/components/directory/SortPills.tsx"
      provides: "Sort option pill buttons"
      exports: ["SortPills"]
  key_links:
    - from: "web/src/components/directory/SearchBar.tsx"
      to: "web/convex/directory.ts"
      via: "calls searchSuggestions query for dropdown"
      pattern: "searchSuggestions"
    - from: "web/src/components/directory/EligibilityFilterBar.tsx"
      to: "web/src/hooks/useScholarshipFilters.ts"
      via: "calls setFilter for 'from' and 'to' params"
      pattern: "setFilter"
    - from: "web/src/components/directory/FilterPanel.tsx"
      to: "web/src/hooks/useScholarshipFilters.ts"
      via: "reads filters and calls setFilter for degree, field, funding, tier"
      pattern: "useScholarshipFilters"
---

<objective>
Build all interactive filter and search components: SearchBar with suggestion dropdown, EligibilityFilterBar (smart nationality + destination bar with compact mode), CountrySelector, NationalityBanner, FilterPanel (sidebar/bottom sheet), FilterChips, QuickFilters, SortPills, and ViewToggle.

Purpose: These components are the interactive layer between the user and the Convex queries. They translate user intent (typing, clicking, selecting) into URL state changes that the filter hooks propagate to the Convex directory query.

Output: All 9 filter/search/sort components ready to be assembled on the directory page.
</objective>

<execution_context>
@/home/siam/.claude/get-shit-done/workflows/execute-plan.md
@/home/siam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06.1-country-eligibility-filtering-university-tier-list-prestige-highlighting/06.1-CONTEXT.md
@.planning/phases/06.1-country-eligibility-filtering-university-tier-list-prestige-highlighting/06.1-UI-SPEC.md
@.planning/phases/06.1-country-eligibility-filtering-university-tier-list-prestige-highlighting/06.1-03-SUMMARY.md
@.planning/phases/06.1-country-eligibility-filtering-university-tier-list-prestige-highlighting/06.1-04-SUMMARY.md

<interfaces>
From web/src/hooks/useScholarshipFilters.ts (from Plan 03):
```typescript
export function useScholarshipFilters(): {
  filters: { q, from: string[], to: string[], degree: string[], field: string[], funding: string[], tier: string[], sort, view, showClosed, showIneligible, closingSoon },
  queryArgs: { ... },
  setFilter: (key: string, value: unknown) => void,
  clearFilters: () => void,
  removeFilter: (key: string, valueToRemove: string) => void,
  activeFilterCount: number,
}
```

From web/src/hooks/useNationalityDetect.ts (from Plan 03):
```typescript
export function useNationalityDetect(): {
  detectedCountry: string | null,
  dismissed: boolean,
  dismiss: () => void,
}
```

From web/src/lib/countries.ts (from Plan 02):
```typescript
export function getCountryFlag(code: string): string;
export function getCountryName(code: string): string;
export function getAllCountries(): Array<{ code: string; name: string; flag: string }>;
export const POPULAR_NATIONALITIES: string[];
export const POPULAR_DESTINATIONS: string[];
```

From web/src/lib/filters.ts (from Plan 02):
```typescript
export const DEGREE_LEVELS: Array<{ value: string; label: string }>;
export const FIELDS_OF_STUDY: readonly string[];
export const FUNDING_TYPES: Array<{ value: string; label: string }>;
export const SORT_OPTIONS: Array<{ value: string; label: string }>;
export function parseCommaSeparated(value: string | undefined): string[];
export function serializeCommaSeparated(values: string[]): string | undefined;
```

From web/convex/directory.ts (from Plan 03):
```typescript
export const searchSuggestions: query({ query: v.string() }) => Array<{ _id, title, host_country, slug }>;
```

Radix UI primitives available (installed in Plan 01):
- @radix-ui/react-popover (SearchBar suggestions)
- @radix-ui/react-select (CountrySelector)
- @radix-ui/react-dialog (FilterPanel mobile bottom sheet)
- @radix-ui/react-toggle-group (QuickFilters, SortPills, ViewToggle)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1a: Build SearchBar and CountrySelector</name>
  <files>web/src/components/directory/SearchBar.tsx, web/src/components/directory/CountrySelector.tsx</files>
  <read_first>
    - web/src/hooks/useScholarshipFilters.ts
    - web/src/lib/countries.ts
    - web/src/lib/filters.ts
    - web/src/components/ui/button.tsx
    - web/src/components/ui/badge.tsx
    - .planning/phases/06.1-country-eligibility-filtering-university-tier-list-prestige-highlighting/06.1-UI-SPEC.md
  </read_first>
  <action>
1. **`SearchBar.tsx`** -- Search input with suggestion dropdown:

Props: `{ onSearch: (query: string) => void, defaultValue?: string }`

Structure:
- Text input with `placeholder="Search scholarships..."`, `aria-label="Search scholarships"`, Lucide `Search` icon on the left
- Styled with neo-brutalism: `border-2 border-border rounded-base bg-secondary-background h-12 px-4 ps-12 w-full text-base font-base focus:ring-2 focus:ring-ring`
- On input change (debounced 300ms), if length >= 2, call `useQuery(api.directory.searchSuggestions, { query })` (use Convex reactive query)
- Show results in Radix `Popover` anchored below the input. Max 5 items.
- Each suggestion item: scholarship title + host country flag on the right. If the suggestion is a filter category (e.g., the search text matches a field of study), show it with a "Filter by: [category]" prefix.
- Clicking a title suggestion navigates to `/scholarships/${slug}`
- Clicking a category suggestion applies the filter via `setFilter`
- Pressing Enter submits the search text via `onSearch(inputValue)` and closes the dropdown
- `aria-expanded`, `aria-controls`, `role="combobox"` for accessibility

Touch target: input height 48px minimum (h-12 = 48px).

2. **`CountrySelector.tsx`** -- Multi-select country picker with flags:

Props: `{ selected: string[], onChange: (codes: string[]) => void, placeholder: string, popularList: string[], maxSelections?: number }`

Structure:
- Display selected countries as removable pills: `[Flag BD Bangladesh x] [Flag DE Germany x]`
- Click to open dropdown (use Radix Popover, not Select -- Select doesn't support multi-select well)
- Dropdown has: search input at top ("Search countries..."), Popular section (showing `popularList` as chips), then full alphabetical country list below
- Each country item: `flag + name`, checkbox indicator on the right
- Selected items have filled checkbox, appear at top of list
- Max selections enforced (default 3 for nationality, unlimited for destinations)
- Close dropdown after each selection (or keep open -- Claude's choice based on UX flow)
- `aria-label` on the button: `placeholder` text

Touch targets: each country item min 44px height.
  </action>
  <verify>
    <automated>cd /home/siam/Personal/ScholarHub/web && test -f src/components/directory/SearchBar.tsx && test -f src/components/directory/CountrySelector.tsx && grep -q "SearchBar" src/components/directory/SearchBar.tsx && grep -q "CountrySelector" src/components/directory/CountrySelector.tsx && grep -q "combobox\|aria-label" src/components/directory/SearchBar.tsx && echo "SearchBar and CountrySelector created"</automated>
  </verify>
  <acceptance_criteria>
    - web/src/components/directory/SearchBar.tsx exports `SearchBar` component
    - web/src/components/directory/SearchBar.tsx imports from "@radix-ui/react-popover"
    - web/src/components/directory/SearchBar.tsx contains `aria-label="Search scholarships"`
    - web/src/components/directory/SearchBar.tsx contains debounce logic (setTimeout or similar, 300ms)
    - web/src/components/directory/CountrySelector.tsx exports `CountrySelector` component
    - web/src/components/directory/CountrySelector.tsx imports `getAllCountries` or `getCountryFlag` from "@/lib/countries"
    - web/src/components/directory/CountrySelector.tsx accepts `selected`, `onChange`, `popularList` props
  </acceptance_criteria>
  <done>SearchBar with suggestion dropdown using Convex searchSuggestions query, and CountrySelector with flag emojis, popular list, and multi-select support are created.</done>
</task>

<task type="auto">
  <name>Task 1b: Build EligibilityFilterBar and NationalityBanner</name>
  <files>web/src/components/directory/EligibilityFilterBar.tsx, web/src/components/directory/NationalityBanner.tsx</files>
  <read_first>
    - web/src/components/directory/CountrySelector.tsx
    - web/src/hooks/useScholarshipFilters.ts
    - web/src/hooks/useNationalityDetect.ts
    - web/src/lib/countries.ts
    - .planning/phases/06.1-country-eligibility-filtering-university-tier-list-prestige-highlighting/06.1-UI-SPEC.md
  </read_first>
  <action>
1. **`EligibilityFilterBar.tsx`** -- Smart nationality + destination bar:

Props: none (reads state from `useScholarshipFilters` hook internally)

States:
- **Empty**: Full two-line layout. Top line: "I'm from" + CountrySelector (nationality, max 3) + "looking to study in" + CountrySelector (destinations, unlimited). Below destination selector: popular destination chips (POPULAR_DESTINATIONS, first 15, clickable to add).
- **Populated**: Same as empty but with selected values shown
- **Compact**: After both nationality and destinations are set, collapse to single line: `"[Flag] Bangladesh -> [Flag][Flag][Flag] [Edit]"`. Click "Edit" expands back to full bar.

Compact state detection: `filters.from.length > 0 && filters.to.length > 0`.

Below the bar: "Also show scholarships you may not qualify for" toggle switch. Uses `setFilter("show_ineligible", !filters.showIneligible)`.

Styling: `bg-secondary-background border-2 border-border rounded-base p-4` (card-like). In compact mode: `py-2 px-4` single line with flex layout.

Save nationality to URL (`from=BD,IN`) AND localStorage via the hook.

2. **`NationalityBanner.tsx`** -- Auto-detect suggestion banner:

Props: none (reads from `useNationalityDetect` hook internally)

Shows when: `detectedCountry !== null && !dismissed && filters.from.length === 0`

Content: Soft yellow/amber background banner: "It looks like you might be in [Flag Country Name]. [Set as nationality]" with a dismiss X button.

Clicking "Set as nationality" calls `setFilter("from", detectedCountry)` and dismisses the banner.
Clicking X calls `dismiss()` (persists to localStorage so it doesn't re-appear).

Styling: `bg-[var(--urgency-warning)]/20 border-2 border-[var(--urgency-warning)] rounded-base p-3` with text-foreground.
  </action>
  <verify>
    <automated>cd /home/siam/Personal/ScholarHub/web && test -f src/components/directory/EligibilityFilterBar.tsx && test -f src/components/directory/NationalityBanner.tsx && grep -q "EligibilityFilterBar" src/components/directory/EligibilityFilterBar.tsx && grep -q "NationalityBanner" src/components/directory/NationalityBanner.tsx && grep -q "compact\|Compact" src/components/directory/EligibilityFilterBar.tsx && echo "EligibilityFilterBar and NationalityBanner created"</automated>
  </verify>
  <acceptance_criteria>
    - web/src/components/directory/EligibilityFilterBar.tsx exports `EligibilityFilterBar` component
    - web/src/components/directory/EligibilityFilterBar.tsx uses `useScholarshipFilters` hook
    - web/src/components/directory/EligibilityFilterBar.tsx has compact mode logic (single line when both from and to are set)
    - web/src/components/directory/EligibilityFilterBar.tsx contains "Also show scholarships you may not qualify for" toggle text
    - web/src/components/directory/NationalityBanner.tsx exports `NationalityBanner` component
    - web/src/components/directory/NationalityBanner.tsx uses `useNationalityDetect` hook
    - web/src/components/directory/NationalityBanner.tsx contains "It looks like you might be in" text
  </acceptance_criteria>
  <done>EligibilityFilterBar with compact mode and NationalityBanner with auto-detect suggestion created, both connected to filter hooks and CountrySelector.</done>
</task>

<task type="auto">
  <name>Task 2: Build FilterPanel, FilterChips, QuickFilters, SortPills, ViewToggle</name>
  <files>web/src/components/directory/FilterPanel.tsx, web/src/components/directory/FilterChips.tsx, web/src/components/directory/QuickFilters.tsx, web/src/components/directory/SortPills.tsx, web/src/components/directory/ViewToggle.tsx</files>
  <read_first>
    - web/src/hooks/useScholarshipFilters.ts
    - web/src/lib/filters.ts
    - web/src/components/ui/button.tsx
    - web/src/components/ui/badge.tsx
    - .planning/phases/06.1-country-eligibility-filtering-university-tier-list-prestige-highlighting/06.1-UI-SPEC.md
  </read_first>
  <action>
1. **`FilterPanel.tsx`** -- Sidebar (desktop) / Bottom sheet (mobile):

Props: none (reads from `useScholarshipFilters` internally)

**Desktop** (>= 1024px): Left sidebar, 280px fixed width, sticky (top: 80px to account for navbar). Contains:
- "Filters" heading (font-heading, text-lg)
- **Degree level**: Multi-select chips from DEGREE_LEVELS. Active chips get accent background + white text. Toggle on/off via `setFilter("degree", ...)`.
- **Field of study**: Searchable multi-select. Text input to filter the 25 FIELDS_OF_STUDY categories. Selected items shown as removable pills below. Uses internal search state (not URL).
- **Funding type**: Multi-select chips from FUNDING_TYPES. All selected values are serialized as comma-separated in URL (`funding=fully_funded,partial`). The `useScholarshipFilters` hook passes the full array to the Convex query.
- **Prestige tier**: Multi-select chips (Gold, Silver, Bronze) with their tier colors as chip backgrounds.
- **Show closed**: Toggle switch. "Show closed scholarships" label.

Each section separated by `border-b border-border` with `py-4` spacing.

**Mobile** (< 1024px): "Filters" button triggers a bottom sheet. Use Radix Dialog with custom styling to act as a bottom sheet:
```tsx
<Dialog.Root>
  <Dialog.Trigger asChild>
    <Button variant="neutral">Filters{activeFilterCount > 0 ? ` (${activeFilterCount})` : ""}</Button>
  </Dialog.Trigger>
  <Dialog.Portal>
    <Dialog.Overlay className="fixed inset-0 bg-overlay z-40" />
    <Dialog.Content className="fixed bottom-0 inset-x-0 z-50 bg-secondary-background border-t-2 border-border rounded-t-base max-h-[85vh] overflow-y-auto p-4 animate-in slide-in-from-bottom">
      {/* Same filter controls as desktop */}
      <Dialog.Close asChild>
        <Button variant="default" className="w-full mt-4">Apply Filters</Button>
      </Dialog.Close>
    </Dialog.Content>
  </Dialog.Portal>
</Dialog.Root>
```

All filter chip touch targets: min 44px height.
All chip `aria-pressed` state for toggle chips.

2. **`FilterChips.tsx`** -- Active filter pills above results:

Props: none (reads from `useScholarshipFilters`)

Shows a horizontal wrapping row of active filter pills. Each pill shows: `"[Category]: [Value] x"` with a remove button. Example: `"Degree: PhD x" | "Funding: Fully Funded x"`.

"Clear all" link at the end (text button, destructive color variant, calls `clearFilters()`).

Only renders when `activeFilterCount > 0`.

Chip structure: `<Badge variant="neutral">Degree: PhD <button aria-label="Remove PhD filter" onClick={() => removeFilter("degree", "phd")}>x</button></Badge>`

3. **`QuickFilters.tsx`** -- Quick filter toggle tabs:

Props: none (reads from `useScholarshipFilters`)

4 toggle chips in a horizontal row using Radix ToggleGroup (type="multiple"):
- "Open Now" -- default active, not a special filter (just the absence of show_closed and closing_soon)
- "Closing Soon" -- sets `closingSoon: true` in URL
- "Fully Funded" -- sets `funding: "fully_funded"` in URL
- "Gold Tier" -- sets `tier: "gold"` in URL

Active state: accent background + white text + neo-brutalism shadow. Inactive: neutral variant.

Combinable with AND logic. Pressing "Closing Soon" + "Fully Funded" shows fully-funded scholarships closing within 30 days.

4. **`SortPills.tsx`** -- Sort option pill buttons:

Props: none (reads from `useScholarshipFilters`)

4 sort pills using Radix ToggleGroup (type="single"):
- Deadline (default active)
- Prestige
- Newest
- Amount

Active pill: accent background, white text, shadow. Inactive: neutral variant.

Each pill calls `setFilter("sort", value)`. Smart defaults: each sort has one direction (no toggle). Deadline = ASC, Prestige = DESC, Newest = DESC, Amount = DESC.

5. **`ViewToggle.tsx`** -- Grid/List icon toggle:

Props: none (reads from `useScholarshipFilters`)

Two icons using Radix ToggleGroup (type="single"):
- Grid icon (Lucide `LayoutGrid`)
- List icon (Lucide `List`)

Active: accent background. Calls `setFilter("view", "grid" | "list")`.

`aria-label`: "Switch to grid view" / "Switch to list view"
  </action>
  <verify>
    <automated>cd /home/siam/Personal/ScholarHub/web && test -f src/components/directory/FilterPanel.tsx && test -f src/components/directory/FilterChips.tsx && test -f src/components/directory/QuickFilters.tsx && test -f src/components/directory/SortPills.tsx && test -f src/components/directory/ViewToggle.tsx && grep -q "FilterPanel" src/components/directory/FilterPanel.tsx && grep -q "FilterChips" src/components/directory/FilterChips.tsx && grep -q "QuickFilters" src/components/directory/QuickFilters.tsx && grep -q "SortPills" src/components/directory/SortPills.tsx && grep -q "ViewToggle" src/components/directory/ViewToggle.tsx && echo "All filter components created"</automated>
  </verify>
  <acceptance_criteria>
    - web/src/components/directory/FilterPanel.tsx exports `FilterPanel` component
    - web/src/components/directory/FilterPanel.tsx imports from "@radix-ui/react-dialog" for mobile bottom sheet
    - web/src/components/directory/FilterPanel.tsx contains degree level, field of study, funding type, prestige tier filter sections
    - web/src/components/directory/FilterPanel.tsx contains "Show closed scholarships" toggle
    - web/src/components/directory/FilterPanel.tsx has responsive logic (sidebar on desktop, bottom sheet on mobile)
    - web/src/components/directory/FilterChips.tsx exports `FilterChips` component
    - web/src/components/directory/FilterChips.tsx contains `removeFilter` call for chip removal
    - web/src/components/directory/FilterChips.tsx contains "Clear all" button
    - web/src/components/directory/QuickFilters.tsx exports `QuickFilters` component
    - web/src/components/directory/QuickFilters.tsx imports from "@radix-ui/react-toggle-group"
    - web/src/components/directory/QuickFilters.tsx contains "Open Now", "Closing Soon", "Fully Funded", "Gold Tier" labels
    - web/src/components/directory/SortPills.tsx exports `SortPills` component
    - web/src/components/directory/SortPills.tsx imports from "@radix-ui/react-toggle-group"
    - web/src/components/directory/SortPills.tsx contains "Deadline", "Prestige", "Newest", "Amount" labels
    - web/src/components/directory/ViewToggle.tsx exports `ViewToggle` component
    - web/src/components/directory/ViewToggle.tsx contains grid and list toggle icons
  </acceptance_criteria>
  <done>All 5 filter/sort/view components built: FilterPanel with sidebar/bottom-sheet responsive layout and multi-select funding type chips, FilterChips with removable active filter pills, QuickFilters with 4 combinable toggle tabs, SortPills with 4 sort options, ViewToggle with grid/list switch. All connected to useScholarshipFilters hook for URL state management.</done>
</task>

</tasks>

<verification>
- SearchBar shows suggestion dropdown after >= 2 chars typed
- CountrySelector supports multi-select with flag emojis and popular list
- EligibilityFilterBar has empty, populated, and compact states
- NationalityBanner shows timezone-detected suggestion
- FilterPanel renders as sidebar on desktop, bottom sheet on mobile
- FilterChips show removable active filter pills
- QuickFilters has 4 combinable toggle tabs
- SortPills has 4 sort options with single selection
- ViewToggle switches between grid and list icons
- All components use useScholarshipFilters hook for state management
</verification>

<success_criteria>
All 9 interactive filter/search components are built and connected to the URL state management system. User interactions (typing, selecting, toggling) update URL search params which flow through the filter hook to Convex query args. The components implement the full interaction contract from the UI-SPEC including compact eligibility bar, searchable multi-selects, toggle chips with AND logic, and mobile bottom sheet.
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-country-eligibility-filtering-university-tier-list-prestige-highlighting/06.1-05-SUMMARY.md`
</output>
