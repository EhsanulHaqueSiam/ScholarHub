---
phase: 02-source-discovery
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - scraping/sources/MUST_HAVE.md
  - scraping/sources/aggregators.json
  - scraping/sources/official_programs.json
  - scraping/sources/government.json
  - scraping/sources/foundations.json
autonomous: false
requirements: [SRCD-01, SRCD-02, SRCD-03, SRCD-04]

must_haves:
  truths:
    - "MUST_HAVE.md lists 20-30 major scholarship programs and top aggregators that must be in the catalog"
    - "aggregators.json contains 40-50 scholarship aggregator sources across waves 1-5"
    - "official_programs.json contains major programs including DAAD, Erasmus, MEXT, Chevening, Fulbright"
    - "government.json and foundations.json contain government and foundation sources at wave 7"
    - "All JSON source files pass JSON Schema validation"
    - "Each entry has scrape_method, wave, scrape_frequency_hours, notes, and geographic_coverage populated"
    - "API-first sources have has_api: true and API URL noted in notes field"
    - "Cloudflare-protected sources have scrape_method: scrapling and CF: yes in notes"
  artifacts:
    - path: "scraping/sources/MUST_HAVE.md"
      provides: "Checklist of 20-30 required sources"
      contains: "- [x]"
    - path: "scraping/sources/aggregators.json"
      provides: "40-50 aggregator sources across waves 1-5"
    - path: "scraping/sources/official_programs.json"
      provides: "Major official scholarship programs at wave 6"
      contains: "DAAD"
    - path: "scraping/sources/government.json"
      provides: "Government scholarship programs at wave 7"
    - path: "scraping/sources/foundations.json"
      provides: "Foundation scholarship sources at wave 7"
  key_links:
    - from: "scraping/sources/aggregators.json"
      to: "scraping/sources/schema.json"
      via: "All entries conform to JSON Schema"
      pattern: "check-jsonschema"
    - from: "scraping/sources/MUST_HAVE.md"
      to: "scraping/sources/*.json"
      via: "Every must-have source appears in a JSON file"
---

<objective>
Populate the source catalog with scholarship sources through systematic AI-assisted research.

Purpose: The catalog is the core deliverable of Phase 2 -- without source entries, there is nothing to scrape in Phase 3. The goal is a comprehensive catalog of aggregators (40-50), official programs, government programs, and foundations, totaling 300+ high-quality sources (soft target 1000+ including future university expansion).
Output: MUST_HAVE.md checklist, 4 populated JSON source files, all passing schema validation.
</objective>

<execution_context>
@/home/siam/.claude/get-shit-done/workflows/execute-plan.md
@/home/siam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-source-discovery/02-CONTEXT.md
@.planning/phases/02-source-discovery/02-RESEARCH.md
@.planning/phases/02-source-discovery/02-01-SUMMARY.md
@.planning/phases/02-source-discovery/02-02-SUMMARY.md

<interfaces>
<!-- Source entry format from Plan 01 -->

From scraping/sources/schema.json (required fields):
```json
{
  "required": ["name", "url", "category", "scrape_method", "scrape_frequency_hours", "wave", "is_active"],
  "properties": {
    "name": { "type": "string" },
    "url": { "type": "string", "format": "uri" },
    "category": { "enum": ["official_program", "university", "aggregator", "government", "foundation"] },
    "scrape_method": { "enum": ["api", "scrape", "scrapling", "rss"] },
    "trust_level": { "enum": ["auto_publish", "needs_review", "blocked"] },
    "scrape_frequency_hours": { "type": "number", "minimum": 1 },
    "wave": { "type": "integer", "minimum": 1, "maximum": 7 },
    "is_active": { "type": "boolean" },
    "auth_required": { "type": "boolean" },
    "has_api": { "type": "boolean" },
    "estimated_volume": { "type": "string" },
    "geographic_coverage": { "type": "array", "items": { "type": "string" } },
    "notes": { "type": "string" }
  }
}
```

Example entry format:
```json
{
  "name": "ScholarshipPortal",
  "url": "https://www.scholarshipportal.com",
  "category": "aggregator",
  "scrape_method": "scrape",
  "trust_level": "needs_review",
  "scrape_frequency_hours": 168,
  "wave": 1,
  "is_active": true,
  "auth_required": false,
  "has_api": false,
  "estimated_volume": "~5000",
  "geographic_coverage": ["europe", "global"],
  "notes": "CF: no | Fields: title, deadline, amount, country, degree | Volume: ~5000 scholarships | Largest EU aggregator"
}
```

From scraping/scripts/validate_sources.py (Plan 02):
```python
# Run to check for duplicates and URL issues:
# cd scraping && uv run python scripts/validate_sources.py
```

From scraping/scripts/stats_sources.py (Plan 02):
```python
# Run to check catalog coverage:
# cd scraping && uv run python scripts/stats_sources.py
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MUST_HAVE.md and populate all source catalog JSON files</name>
  <files>scraping/sources/MUST_HAVE.md, scraping/sources/aggregators.json, scraping/sources/official_programs.json, scraping/sources/government.json, scraping/sources/foundations.json</files>
  <read_first>
    - scraping/sources/schema.json (JSON Schema that all entries must conform to)
    - scraping/sources/README.md (template entry and valid values reference)
    - scraping/tests/fixtures/sample_sources.json (example entries for format reference)
    - .planning/phases/02-source-discovery/02-CONTEXT.md (discovery method, wave system, quality bar)
  </read_first>
  <action>
    **Step 1: Create scraping/sources/MUST_HAVE.md**
    A checklist of 20-30 essential sources that MUST be in the catalog. Include:

    **Top Aggregators (must include all):**
    - ScholarshipPortal (scholarshipportal.com)
    - Scholars4Dev (scholars4dev.com)
    - IEFA (iefa.org)
    - Scholarship Positions (scholarship-positions.com)
    - Opportunities For Africans (opportunitiesforafricans.com)
    - StudyPortals (studyportals.com)
    - FindAMasters / FindAPhD (findamasters.com / findaphd.com)
    - ScholarshipDB (scholarshipdb.net)
    - After School Africa (afterschoolafrica.com)
    - Top Universities (topuniversities.com/scholarships)
    - Mariam Assefa Fund / Opportunity Desk (opportunitydesk.org)

    **Must-have Official Programs:**
    - DAAD (daad.de)
    - Erasmus+ / Erasmus Mundus (ec.europa.eu)
    - MEXT Japan (studyinjapan.go.jp)
    - Chevening UK (chevening.org)
    - Fulbright USA (foreign.fulbrightonline.org)
    - Commonwealth Scholarships (cscuk.org.uk)
    - Australia Awards (australiaawards.gov.au)
    - Chinese Government Scholarship / CSC (campuschina.org)
    - KGSP Korea (studyinkorea.go.kr)
    - Stipendium Hungaricum (stipendiumhungaricum.hu)
    - Turkiye Burslari (turkiyeburslari.gov.tr)
    - Swiss Government Excellence Scholarships (sbfi.admin.ch)
    - New Zealand Scholarships (mfat.govt.nz)
    - Aga Khan Foundation (akdn.org)
    - Gates Cambridge (gatescambridge.org)

    Format: `- [x] SourceName (url) -- in file.json` once cataloged.

    **Step 2: Create scraping/sources/aggregators.json**
    Research and catalog 40-50 scholarship aggregator websites. Organize by wave:
    - Wave 1 (10-12): Top-tier global aggregators (ScholarshipPortal, Scholars4Dev, IEFA, StudyPortals, FindAMasters, FindAPhD, TopUniversities, ScholarshipDB)
    - Wave 2 (8-10): Regional major aggregators (Opportunities For Africans, After School Africa, Scholarship Positions, OpportunityDesk, etc.)
    - Wave 3 (8-10): Specialized/niche aggregators (by field, by degree level, by funding type)
    - Wave 4 (6-8): Smaller/regional aggregators
    - Wave 5 (5-8): Remaining aggregators (newer, smaller, less established)

    For each entry:
    - `url`: HTTPS URL to main scholarships listing page
    - `scrape_method`: `"api"` if API known, `"scrape"` for standard HTML, `"scrapling"` if CF-protected, `"rss"` if RSS feed available
    - `has_api`: `true` if API endpoint discovered
    - `scrape_frequency_hours`: 168 (weekly) for most, 24-72 for high-volume/frequently-updated
    - `estimated_volume`: rough count string like `"~5000"`, `"~1000"`, `"~200"`
    - `geographic_coverage`: array of regions covered (use: `"global"`, `"europe"`, `"asia"`, `"africa"`, `"americas"`, `"oceania"`, `"middle_east"`)
    - `notes`: Semi-structured with key prefixes. Always include: `Fields:` (what data the source provides), `Volume:` if known. Add `CF: yes` if Cloudflare-protected. Add `API: <endpoint>` if has_api. Add `Auth: <type>` if auth_required.
    - `trust_level`: `"needs_review"` for all
    - `is_active`: `true` for all
    - `auth_required`: `true` only if registration/API key needed to access data

    **Step 3: Create scraping/sources/official_programs.json**
    Research and catalog major official scholarship programs (wave 6). Target 80-120 entries covering:
    - All MUST_HAVE official programs
    - Government-backed bilateral scholarship programs (by country)
    - Major university-system-wide programs (Erasmus Mundus joint degrees, etc.)
    - Regional scholarship bodies (OFID, IsDB, ADB, etc.)

    Each entry at wave 6, category `"official_program"`. scrape_frequency_hours: 336-720 (bi-weekly to monthly, these change slowly). Notes should indicate available data fields and any API.

    **Step 4: Create scraping/sources/government.json**
    Research and catalog government scholarship programs (wave 7). Target 40-60 entries:
    - Country-level government scholarship portals
    - Ministry of education scholarship pages
    - Government development agency scholarships (JICA, GIZ, KOICA, USAID-linked, etc.)

    Each entry at wave 7, category `"government"`.

    **Step 5: Create scraping/sources/foundations.json**
    Research and catalog foundation/NGO scholarship sources (wave 7). Target 30-50 entries:
    - Major foundations (Gates Cambridge, Aga Khan, Ford Foundation, etc.)
    - NGO-backed scholarship programs
    - Corporate scholarship programs (Google, Microsoft, Mastercard Foundation, etc.)

    Each entry at wave 7, category `"foundation"`.

    **Step 6: Validate all files**
    Run: `cd scraping && uv run check-jsonschema --schemafile sources/schema.json sources/aggregators.json sources/official_programs.json sources/government.json sources/foundations.json`
    Fix any validation errors.

    **Step 7: Run stats to verify coverage**
    Run: `cd scraping && uv run python scripts/stats_sources.py`
    Verify counts are reasonable and all categories are represented.

    **Step 8: Update MUST_HAVE.md**
    Go through the MUST_HAVE.md checklist and mark all cataloged sources as `[x]` with their file location. Any uncataloged must-haves should remain as `[ ]` with a note explaining why (source defunct, requires further research, etc.).
  </action>
  <verify>
    <automated>cd /home/siam/Personal/ScholarHub/scraping && uv run check-jsonschema --schemafile sources/schema.json sources/aggregators.json sources/official_programs.json sources/government.json sources/foundations.json</automated>
  </verify>
  <acceptance_criteria>
    - scraping/sources/MUST_HAVE.md exists and contains at least 20 entries
    - scraping/sources/MUST_HAVE.md contains "DAAD" and "Fulbright" and "Chevening" and "Erasmus" and "MEXT"
    - scraping/sources/MUST_HAVE.md contains "ScholarshipPortal" and "Scholars4Dev" and "IEFA"
    - scraping/sources/aggregators.json is a valid JSON array with 40+ entries
    - scraping/sources/aggregators.json contains entries with wave values 1 through 5
    - scraping/sources/official_programs.json is a valid JSON array with 50+ entries
    - scraping/sources/official_programs.json contains "DAAD" and "Fulbright" and "Chevening"
    - scraping/sources/government.json is a valid JSON array with 30+ entries
    - scraping/sources/foundations.json is a valid JSON array with 20+ entries
    - `check-jsonschema --schemafile sources/schema.json sources/aggregators.json sources/official_programs.json sources/government.json sources/foundations.json` exits 0
    - All entries have scrape_method, wave, scrape_frequency_hours, is_active, and trust_level fields
    - No entry has category "university" (universities deferred per CONTEXT.md)
  </acceptance_criteria>
  <done>
    MUST_HAVE.md lists 20-30 essential sources, all checked off. 4 JSON files contain 200+ total sources across all non-university categories. All pass JSON Schema validation.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User reviews source catalog completeness and quality</name>
  <files>scraping/sources/MUST_HAVE.md, scraping/sources/aggregators.json, scraping/sources/official_programs.json, scraping/sources/government.json, scraping/sources/foundations.json</files>
  <what-built>Source catalog with 200+ scholarship sources across 4 JSON files (aggregators, official programs, government, foundations), plus a MUST_HAVE.md checklist. All entries validated against JSON Schema.</what-built>
  <how-to-verify>
    1. Review the MUST_HAVE.md checklist -- are all critical sources you care about listed and checked off?
    2. Run `cd scraping && uv run python scripts/stats_sources.py` to see catalog coverage breakdown
    3. Spot-check a few entries in each JSON file -- are the URLs real? Are scrape_methods reasonable? Are notes helpful?
    4. Run `cd scraping && uv run check-jsonschema --schemafile sources/schema.json sources/aggregators.json sources/official_programs.json sources/government.json sources/foundations.json` to confirm validation passes
    5. Check if any important sources are missing that you want added
    6. Note: The 1000+ target is a soft goal. Quality aggregator coverage matters more than raw count. Aggregators like ScholarshipPortal index thousands of scholarships each, so 50 good aggregators may cover more ground than 500 individual sources.
  </how-to-verify>
  <action>Present the catalog to the user for review. Show stats output and MUST_HAVE.md status.</action>
  <verify>User types "approved"</verify>
  <done>User has reviewed and approved the source catalog.</done>
  <resume-signal>Type "approved" to complete Phase 2, or describe sources to add/remove/modify</resume-signal>
</task>

</tasks>

<verification>
- `cd scraping && uv run check-jsonschema --schemafile sources/schema.json sources/aggregators.json sources/official_programs.json sources/government.json sources/foundations.json` -- all files valid
- `cd scraping && uv run python scripts/stats_sources.py` -- shows 200+ total sources with all categories represented
- MUST_HAVE.md has all critical sources checked off
- No university-category entries (deferred per CONTEXT.md)
- Every entry has a populated notes field with relevant metadata
</verification>

<success_criteria>
- MUST_HAVE.md contains 20-30 entries, all major programs and top aggregators checked off
- 200+ total source entries across 4 JSON files (soft target, quality over quantity)
- All entries pass JSON Schema validation
- Aggregators span waves 1-5, official programs at wave 6, government + foundations at wave 7
- Each entry has meaningful notes (data fields available, volume estimate, CF status, API availability)
- User approves the catalog at checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/02-source-discovery/02-03-SUMMARY.md`
</output>
