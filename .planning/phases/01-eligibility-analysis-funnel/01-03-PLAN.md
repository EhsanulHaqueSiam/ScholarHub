---
phase: 01-eligibility-analysis-funnel
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/index.css
  - web/src/components/ui/badge.tsx
  - web/src/components/layout/Navbar.tsx
  - web/src/components/eligibility/EligibilityCTA.tsx
autonomous: true
requirements:
  - D-03
  - D-04
  - D-05

must_haves:
  truths:
    - "Match tier colors (strong green, good blue, partial amber, possible gray) exist as CSS custom properties"
    - "Badge component has 4 new match tier variants"
    - "Navigation bar has an Eligibility link on both desktop and mobile"
    - "EligibilityCTA button component exists for homepage/nav use"
  artifacts:
    - path: "web/src/index.css"
      provides: "Match tier CSS custom properties (12 new tokens)"
      contains: "--match-strong-bg"
    - path: "web/src/components/ui/badge.tsx"
      provides: "4 new badge variants: matchStrong, matchGood, matchPartial, matchPossible"
      contains: "matchStrong"
    - path: "web/src/components/layout/Navbar.tsx"
      provides: "Eligibility nav link on desktop and mobile"
      contains: "/eligibility"
    - path: "web/src/components/eligibility/EligibilityCTA.tsx"
      provides: "CTA button component with accent variant"
      exports: ["EligibilityCTA"]
  key_links:
    - from: "web/src/components/ui/badge.tsx"
      to: "web/src/index.css"
      via: "Tailwind classes reference CSS custom property tokens"
      pattern: "match-strong|match-good|match-partial|match-possible"
    - from: "web/src/components/layout/Navbar.tsx"
      to: "/eligibility"
      via: "NavLink component with to prop"
      pattern: 'to="/eligibility"'
---

<objective>
Add match tier design tokens to the CSS, extend the Badge component with tier variants, add the Eligibility link to navigation, and create the CTA button component.

Purpose: These design system extensions and navigation changes are prerequisites for the wizard and results UI. They have zero code dependencies on other plans and can run in Wave 1.

Output: Updated CSS with 12 tier tokens, Badge with 4 new variants, Navbar with Eligibility link, EligibilityCTA component
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-eligibility-analysis-funnel/01-CONTEXT.md
@.planning/phases/01-eligibility-analysis-funnel/01-UI-SPEC.md

@web/src/index.css
@web/src/components/ui/badge.tsx
@web/src/components/layout/Navbar.tsx

<interfaces>
From web/src/components/ui/badge.tsx (current):
```typescript
import { cva, type VariantProps } from "class-variance-authority";
const badgeVariants = cva("...", { variants: { variant: { ... } } });
export function Badge({ variant, className, ...props })
```

From web/src/components/layout/Navbar.tsx (current):
```typescript
function NavLink({ to, exact, children }: { to: string; exact?: boolean; children: React.ReactNode })
// Desktop nav links in <div className="hidden md:flex items-center gap-3">
// Mobile nav links in dropdown <div className="flex flex-col px-4 py-3 gap-1">
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add match tier CSS tokens and Badge variants</name>
  <files>
    web/src/index.css
    web/src/components/ui/badge.tsx
  </files>
  <read_first>
    web/src/index.css
    web/src/components/ui/badge.tsx
    .planning/phases/01-eligibility-analysis-funnel/01-UI-SPEC.md
  </read_first>
  <action>
    **Step 1: Add match tier CSS custom properties to web/src/index.css**

    Find the existing `:root` or `@theme` section in index.css. Add these 12 new CSS custom properties for match tiers (per UI-SPEC "New Semantic Colors" table):

    Light mode (in the base/light section):
    ```css
    --match-strong-bg: oklch(90% 0.08 150);
    --match-strong-border: oklch(55% 0.16 150);
    --match-strong-badge: oklch(62% 0.18 150);
    --match-good-bg: oklch(92% 0.06 230);
    --match-good-border: oklch(55% 0.14 230);
    --match-good-badge: oklch(60% 0.16 230);
    --match-partial-bg: oklch(93% 0.06 85);
    --match-partial-border: oklch(60% 0.14 85);
    --match-partial-badge: oklch(72% 0.16 85);
    --match-possible-bg: oklch(95% 0.01 0);
    --match-possible-border: oklch(70% 0.02 0);
    --match-possible-badge: oklch(65% 0.03 0);
    ```

    Dark mode (in the .dark section):
    ```css
    --match-strong-bg: oklch(24% 0.06 150);
    --match-strong-border: oklch(45% 0.14 150);
    --match-strong-badge: oklch(55% 0.16 150);
    --match-good-bg: oklch(23% 0.04 230);
    --match-good-border: oklch(45% 0.12 230);
    --match-good-badge: oklch(52% 0.14 230);
    --match-partial-bg: oklch(24% 0.04 85);
    --match-partial-border: oklch(48% 0.12 85);
    --match-partial-badge: oklch(60% 0.14 85);
    --match-possible-bg: oklch(22% 0.01 0);
    --match-possible-border: oklch(42% 0.02 0);
    --match-possible-badge: oklch(50% 0.03 0);
    ```

    Also add the match indicator colors (per UI-SPEC "Match Indicator Colors"):
    ```css
    --match-check: oklch(62% 0.18 150);
    --match-cross: oklch(60% 0.20 25);
    --match-unknown: oklch(65% 0.01 0);
    ```

    **Step 2: Add 4 new badge variants to web/src/components/ui/badge.tsx**

    Add these variants inside the existing `badgeVariants` cva `variant` object (per UI-SPEC "New Badge Variants"):

    ```typescript
    matchStrong: "bg-[var(--match-strong-badge)] text-white border-[var(--match-strong-border)] font-heading",
    matchGood: "bg-[var(--match-good-badge)] text-white border-[var(--match-good-border)] font-heading",
    matchPartial: "bg-[var(--match-partial-badge)] text-black border-[var(--match-partial-border)] font-heading",
    matchPossible: "bg-[var(--match-possible-badge)] text-white border-[var(--match-possible-border)] font-heading",
    ```
  </action>
  <verify>
    <automated>cd /home/siam/Personal/ScholarHub/web && grep -c "match-strong-bg\|match-good-bg\|match-partial-bg\|match-possible-bg" src/index.css && grep -c "matchStrong\|matchGood\|matchPartial\|matchPossible" src/components/ui/badge.tsx && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - web/src/index.css contains `--match-strong-bg: oklch(90% 0.08 150)`
    - web/src/index.css contains `--match-good-bg: oklch(92% 0.06 230)`
    - web/src/index.css contains `--match-partial-bg: oklch(93% 0.06 85)`
    - web/src/index.css contains `--match-possible-bg: oklch(95% 0.01 0)`
    - web/src/index.css contains `--match-check: oklch(62% 0.18 150)`
    - web/src/index.css contains `--match-cross: oklch(60% 0.20 25)`
    - web/src/index.css dark section contains `--match-strong-bg: oklch(24% 0.06 150)`
    - web/src/components/ui/badge.tsx contains `matchStrong:`
    - web/src/components/ui/badge.tsx contains `matchGood:`
    - web/src/components/ui/badge.tsx contains `matchPartial:`
    - web/src/components/ui/badge.tsx contains `matchPossible:`
  </acceptance_criteria>
  <done>12 match tier CSS tokens defined for both light and dark modes. 3 match indicator color tokens added. Badge component has 4 new tier variants.</done>
</task>

<task type="auto">
  <name>Task 2: Add Eligibility nav link and create CTA component</name>
  <files>
    web/src/components/layout/Navbar.tsx
    web/src/components/eligibility/EligibilityCTA.tsx
  </files>
  <read_first>
    web/src/components/layout/Navbar.tsx
    web/src/components/ui/button.tsx
    .planning/phases/01-eligibility-analysis-funnel/01-UI-SPEC.md
  </read_first>
  <action>
    **Step 1: Add Eligibility link to Navbar per D-03**

    In `web/src/components/layout/Navbar.tsx`:

    Desktop nav section (inside `<div className="hidden md:flex items-center gap-3">`), add BEFORE the dark mode toggle button:
    ```tsx
    <NavLink to="/eligibility" exact>
      Eligibility
    </NavLink>
    ```

    Mobile dropdown section (inside the mobile menu `<div className="flex flex-col px-4 py-3 gap-1">`), add as a new entry after the Guide link:
    ```tsx
    <Link
      to="/eligibility"
      activeOptions={{ exact: true }}
      className="font-heading text-sm rounded-base border-2 border-transparent px-3 py-2.5 min-h-[44px] flex items-center transition-all"
      activeProps={{ className: "border-border bg-main text-main-foreground" }}
      onClick={() => setMenuOpen(false)}
    >
      Eligibility
    </Link>
    ```

    **Step 2: Create EligibilityCTA component**

    Create `web/src/components/eligibility/EligibilityCTA.tsx`:

    ```tsx
    import { Link } from "@tanstack/react-router";
    import { Button } from "@/components/ui/button";
    import { Sparkles } from "lucide-react";

    interface EligibilityCTAProps {
      size?: "default" | "lg";
      className?: string;
    }

    export function EligibilityCTA({ size = "lg", className }: EligibilityCTAProps) {
      return (
        <Link to="/eligibility">
          <Button variant="accent" size={size} className={className}>
            <Sparkles className="size-4 mr-2" />
            Check Your Eligibility
          </Button>
        </Link>
      );
    }
    ```

    Per UI-SPEC Copywriting Contract: CTA text is "Check Your Eligibility". Per UI-SPEC Color: accent variant is reserved for the homepage CTA. Use the Sparkles icon from lucide-react for visual interest.
  </action>
  <verify>
    <automated>cd /home/siam/Personal/ScholarHub/web && grep -c '"/eligibility"' src/components/layout/Navbar.tsx && test -f src/components/eligibility/EligibilityCTA.tsx && echo "CTA exists" && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - web/src/components/layout/Navbar.tsx contains `to="/eligibility"` (at least 2 occurrences: desktop + mobile)
    - web/src/components/layout/Navbar.tsx desktop section contains `<NavLink to="/eligibility"`
    - web/src/components/eligibility/EligibilityCTA.tsx exists
    - web/src/components/eligibility/EligibilityCTA.tsx contains `variant="accent"`
    - web/src/components/eligibility/EligibilityCTA.tsx contains `Check Your Eligibility`
    - web/src/components/eligibility/EligibilityCTA.tsx contains `import { Link } from "@tanstack/react-router"`
  </acceptance_criteria>
  <done>Navbar shows "Eligibility" link on desktop and mobile. EligibilityCTA component renders accent-variant button linking to /eligibility with "Check Your Eligibility" text.</done>
</task>

</tasks>

<verification>
```bash
cd /home/siam/Personal/ScholarHub/web && npx tsc --noEmit 2>&1 | head -30
```
Verify no TypeScript errors from the changes.
</verification>

<success_criteria>
- 12 match tier CSS tokens in light mode + 12 in dark mode
- 3 match indicator color tokens
- Badge has matchStrong/matchGood/matchPartial/matchPossible variants
- Navbar has Eligibility link visible on both desktop and mobile
- EligibilityCTA component created with accent styling
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-eligibility-analysis-funnel/01-03-SUMMARY.md`
</output>
