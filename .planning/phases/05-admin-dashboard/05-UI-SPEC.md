---
phase: 5
slug: admin-dashboard
status: draft
shadcn_initialized: true
preset: default (neutral base)
created: 2026-03-22
---

# Phase 5 — UI Design Contract

> Visual and interaction contract for the admin dashboard. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn |
| Preset | default style, neutral baseColor, cssVariables: true |
| Component library | Radix UI primitives + CVA variants |
| Icon library | lucide-react 0.577.0 |
| Font (heading) | Archivo Black, weight 400 |
| Font (body) | Inter, weight 400 |

Source: `web/components.json`, `web/src/index.css`

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline badge padding, checkbox-to-label gap |
| sm | 8px | Compact row padding, toolbar button gaps |
| md | 16px | Default element spacing, queue row inline padding |
| lg | 24px | Section padding, card content padding (existing px-6) |
| xl | 32px | Stats card grid gap, panel header/footer padding |
| 2xl | 48px | Gap between stats bar and review queue |
| 3xl | 64px | Not used in admin layout (admin is dense) |

Exceptions:
- Floating bulk action bar: 12px vertical padding (compact bar needs tighter spacing)
- Slide-out edit panel: 24px (p-6) internal padding, matching existing Card content padding
- Checkbox touch targets: 40px minimum (10px padding around 20px checkbox area)

---

## Typography

| Role | Size | Weight | Line Height | Tailwind Class | Usage |
|------|------|--------|-------------|----------------|-------|
| Body | 14px | 400 (Inter) | 1.5 | text-sm | Queue row text, form labels, table cells, panel body text |
| Label | 12px | 400 (Inter) | 1.4 | text-xs | Badge text, metadata, timestamps, revision entries |
| Heading | 20px | 400 (Archivo Black) | 1.2 | text-xl font-heading | Stats card titles, panel title, section headings |
| Display | 28px | 400 (Archivo Black) | 1.1 | text-2xl font-heading | Stats card numbers (total, pending count, etc.) |

Notes:
- Admin dashboard uses 14px body (text-sm) as default, not 16px. Admin UIs favor density over readability.
- All heading text uses `font-heading` class (Archivo Black). All body text uses `font-base` class (Inter).
- Existing pattern confirmed: ScholarshipCard uses text-xl for titles, text-sm for body, text-xs for metadata.

Source: existing ScholarshipCard.tsx, Badge component (text-xs), Navbar (text-sm, text-xl)

---

## Color

| Role | Token | Light Value | Dark Value | Usage |
|------|-------|-------------|------------|-------|
| Dominant (60%) | --background | oklch(96% 0.008 75) | oklch(16% 0.015 280) | Page background, admin layout canvas |
| Secondary (30%) | --secondary-background | oklch(99.5% 0 0) | oklch(21% 0.02 275) | Queue rows, stats cards, edit panel bg, table headers |
| Accent (10%) | --main | oklch(55% 0.24 264) | oklch(58% 0.20 264) | See accent reserved-for list below |
| Destructive | --destructive | oklch(52% 0.24 27) | oklch(60% 0.20 25) | Reject button, delete actions only |
| Foreground | --foreground | oklch(12% 0.02 285) | oklch(93% 0.005 90) | All text |
| Border | --border | oklch(0% 0 0) | oklch(52% 0.03 275) | All borders (neo-brutalism 2px solid) |

### Admin-Specific Status Colors

Reuse existing urgency badge variants for scholarship statuses:

| Status | Badge Variant | Rationale |
|--------|--------------|-----------|
| pending_review | urgencyWarning | Amber -- needs attention, not urgent |
| published | urgencyOpen | Green -- live and visible |
| rejected | urgencyCritical | Red -- blocked from publishing |
| archived | urgencyClosed | Gray -- inactive reference |
| draft | neutral | Muted -- not yet processed |

### Source Trust Level Colors

| Trust Level | Badge Variant | Rationale |
|-------------|--------------|-----------|
| auto_publish | urgencyOpen | Green -- fully trusted |
| needs_review | urgencyWarning | Amber -- requires human review |
| blocked | urgencyCritical | Red -- blocked from pipeline |

### Accent Reserved For

The accent color (--main, blue-violet) is reserved exclusively for:
1. Primary action buttons: "Approve" single action, "Save Changes" in edit panel
2. Active tab indicator in status tabs (Pending Review / Published / Rejected / Archived / All)
3. Selected checkbox fill color
4. Floating bulk action bar "Approve N selected" button background
5. Stats card accent borders (left border highlight on stats cards)

Never use accent for: status badges (use urgency variants), destructive actions (use --destructive), neutral/informational elements.

Source: existing `web/src/index.css` CSS variables, Badge/Button CVA variants

---

## Component Inventory

### New Components (Phase 5)

| Component | Location | Pattern | Key Props |
|-----------|----------|---------|-----------|
| StatsBar | `web/src/components/admin/StatsBar.tsx` | 4-column grid of stat cards | stats: AdminStats |
| StatCard | `web/src/components/admin/StatCard.tsx` | Card with number + label + trend | label, value, icon, accentBorder? |
| ReviewQueue | `web/src/components/admin/ReviewQueue.tsx` | Tabs + list + pagination | status, onSelectScholarship |
| QueueRow | `web/src/components/admin/QueueRow.tsx` | Expandable row (compact/expanded) | scholarship, isExpanded, isSelected, onToggle, onSelect |
| BulkActionBar | `web/src/components/admin/BulkActionBar.tsx` | Fixed bottom floating bar | selectedCount, onApprove, onReject, onClear |
| EditPanel | `web/src/components/admin/EditPanel.tsx` | Radix Dialog as right slide-out sheet | open, scholarshipId, onClose |
| EditForm | `web/src/components/admin/EditForm.tsx` | All-fields form inside EditPanel | scholarship, onSave |
| EditorialEditor | `web/src/components/admin/EditorialEditor.tsx` | TipTap WYSIWYG wrapper | content, onChange |
| RevisionHistory | `web/src/components/admin/RevisionHistory.tsx` | Collapsible timeline of changes | scholarshipId |
| SourceTrustManager | `web/src/components/admin/SourceTrustManager.tsx` | Source list with trust level dropdowns | sources |
| DuplicateBadge | `web/src/components/admin/DuplicateBadge.tsx` | Warning badge for possible duplicates | matchStatus |

### Reused Components (Existing)

| Component | Source | Usage in Admin |
|-----------|--------|---------------|
| Card, CardHeader, CardContent | `web/src/components/ui/card.tsx` | Stats cards (prestige="unranked"), edit panel sections |
| Badge | `web/src/components/ui/badge.tsx` | Status badges (urgency variants), trust level badges, duplicate warning |
| Button | `web/src/components/ui/button.tsx` | Approve (default), Reject (add destructive variant), Save, Close Panel (neutral) |
| Pagination | `web/src/components/directory/Pagination.tsx` | Queue pagination (client-side, 20 per page) |

### New Button Variant Needed

Add a `destructive` variant to the existing Button CVA:

```
destructive: "text-white bg-destructive border-2 border-border shadow-shadow hover:translate-x-boxShadowX hover:translate-y-boxShadowY hover:shadow-none"
```

Usage: "Reject" button, "Reject N selected" in bulk action bar.

---

## Layout Contract

### Admin Route Layout (`/admin`)

```
+----------------------------------------------------------+
| Admin Header (no public Navbar)                     [theme] |
| "ScholarHub Admin" logo text     [Source Trust] [Queue]    |
+----------------------------------------------------------+
| Stats Bar: 4 cards in a row                                |
| [Total: 482] [Pending: 37] [Published Today: 12] [Sources]|
+----------------------------------------------------------+
| Tabs: [Pending Review (37)] [Published] [Rejected]         |
|        [Archived] [All]                                    |
+----------------------------------------------------------+
| [ ] Select All (visible) | Sort: Trust Level desc          |
+----------------------------------------------------------+
| [ ] Row 1: Title | Country | Source | Degrees | DL | Badge |
|     [expanded: full details + Approve / Reject / Edit]     |
| [ ] Row 2: Title | Country | Source | Degrees | DL | Badge |
| [ ] Row 3: ...                                             |
+----------------------------------------------------------+
| Pagination: < 1 2 3 ... 5 >                               |
+----------------------------------------------------------+
```

### Admin Header

- No public Navbar. Admin uses its own minimal header.
- Height: 48px (h-12). Tighter than public Navbar (h-16).
- Left: "ScholarHub Admin" in font-heading text-lg.
- Right: dark mode toggle (reuse useTheme pattern from Navbar).
- Background: --secondary-background. Bottom border: 2px solid --border.
- No hamburger menu. Admin is desktop-only (min-width: 1024px).

### Stats Bar

- 4-column CSS grid: `grid grid-cols-4 gap-8`
- Each stat card: Card with prestige="unranked", left accent border 4px solid --main
- Layout inside card: Display number top, Label below, optional trend arrow
- Min-height: 96px per card

### Review Queue

- Radix Tabs for status switching: Pending Review (with count badge), Published, Rejected, Archived, All
- Tab trigger: font-heading text-sm, 2px bottom border on active (--main color)
- Below tabs: toolbar row with "Select All" checkbox + sort indicator
- Queue rows below toolbar

### QueueRow (Compact State)

- Single horizontal row, min-height: 56px
- Layout: `[checkbox 40px] [title flex-1] [country 80px] [source 120px] [degrees 100px] [deadline 100px] [status badge 100px] [expand chevron 32px]`
- Left checkbox: 20px with 10px padding each side for 40px touch target
- Expand chevron: 32px touch target with tooltip "Expand row" (collapsed) / "Collapse row" (expanded)
- Background: --secondary-background
- Bottom border: 1px solid --border (thinner than card borders for density)
- Hover: translate-y-[-1px] with subtle shadow
- If has_possible_duplicate: amber DuplicateBadge appears next to title

### QueueRow (Expanded State)

- Row expands downward, showing all scholarship fields in a 2-column grid
- Left column: title, description (truncated to 3 lines), host_country, degree_levels, fields_of_study, eligibility
- Right column: funding_amount, application_url (link), application_deadline, editorial_notes (preview), source provenance with trust badges
- Action buttons at bottom-right: [Approve (default)] [Reject (destructive)] [Edit (neutral)]
- Expand/collapse animation: max-height transition, 200ms ease-out
- Expanded area padding: 16px (p-4)
- Expanded area bg: same as compact row, with a 1px top divider

### EditPanel (Slide-Out Sheet)

- Radix Dialog configured as Sheet (right-aligned)
- Width: 40% of viewport, min-width: 480px, max-width: 640px
- Full viewport height (inset-y-0 right-0)
- Background: --secondary-background
- Left border: 2px solid --border
- Shadow: `-4px 4px 0px 0px var(--border)` (reverse shadow, leftward)
- Overlay: --overlay (oklch(0% 0 0 / 0.8))
- Slide-in animation: translate-x from 100% to 0%, 300ms ease-out
- Internal padding: 24px (p-6)
- Close button: top-right, X icon, 40px touch target, tooltip "Close panel"
- Panel sections: sticky header (title + close), scrollable body, sticky footer (Save + Close Panel)

### EditForm

- Vertical form layout inside EditPanel scrollable area
- Each field: label (text-xs font-heading uppercase tracking-wide) + input
- Text inputs: 2px border, rounded-base, h-10 px-3, bg-background
- Textarea (description): min-h-[120px], resize-y
- Select dropdowns (status, degree_levels): native select with neo-brutalism border styling
- Degree levels, fields_of_study: multi-select checkboxes in a collapsible section
- Editorial notes: TipTap EditorialEditor component (see below)
- Save button: Button variant="default" (accent). Close Panel: Button variant="neutral"
- Footer: sticky at panel bottom, bg-secondary-background, top border 2px, py-4 px-6

### EditorialEditor (TipTap)

- TipTap with StarterKit + Link + Placeholder extensions
- Toolbar: horizontal bar with icon buttons for Bold, Italic, Link, BulletList, OrderedList, Heading (H2/H3)
- Toolbar buttons: 32px square, 2px border, rounded-base, --secondary-background bg
- Each toolbar button has a tooltip describing its action (see Tooltip Declarations below)
- Active toolbar button: --main bg, --main-foreground text
- Editor content area: min-height 120px, padding 16px, bg-background
- Wrapper: 2px border --border, rounded-base
- Placeholder text: "Add tips for applicants..." in --foreground at 40% opacity

### BulkActionBar (Floating)

- Fixed to bottom of viewport: `fixed bottom-0 inset-x-0 z-40`
- Appears only when selectedCount > 0
- Slide-up animation: translate-y from 100% to 0%, 200ms ease-out
- Background: --foreground (inverted for contrast -- dark bar in light mode, light bar in dark mode)
- Text color: --background (inverted)
- Height: 56px
- Layout: `"N scholarships selected" [Approve N selected (accent)] [Reject N selected (destructive)] [Clear selection (ghost)]`
- Border-top: 2px solid --border
- Shadow: `0 -4px 0 0 var(--border)` (upward neo-brutalism shadow)
- Max-width: 1280px, centered

### RevisionHistory

- Collapsible section inside EditPanel, below the form
- Toggle: "Change History (N revisions)" with chevron
- When expanded: vertical timeline, newest first
- Each entry: `[timestamp text-xs] [field_name font-heading text-xs] "old_value" -> "new_value"`
- Timeline connector: 2px solid --border vertical line on the left
- Dot on timeline: 8px circle, filled --main
- Max-height: 300px with overflow-y-auto

### SourceTrustManager

- Accessible via tab/link in admin header or as a separate section below the queue
- Table layout: `[Source Name] [Category badge] [Current Trust] [Change dropdown]`
- Trust level dropdown: native select with urgency-colored badge next to it
- "Apply" button triggers retroactive re-evaluation (D-17)
- Shows count of affected scholarships before confirming

### Responsive Behavior

- Admin dashboard is desktop-only. Minimum supported width: 1024px.
- Below 1024px: show a centered message "Admin dashboard requires a desktop browser (1024px minimum)."
- No mobile hamburger, no responsive breakpoints for admin components.
- EditPanel does NOT overlay on mobile -- it is desktop-only.

---

## Tooltip Declarations

All icon-only interactive elements require a visible tooltip on hover/focus. Use Radix Tooltip (200ms open delay, 8px offset) for consistent behavior.

| Element | Tooltip Text | Location |
|---------|-------------|----------|
| EditPanel close X button | "Close panel" | EditPanel sticky header, top-right |
| QueueRow expand chevron (collapsed) | "Expand row" | QueueRow compact state, rightmost column |
| QueueRow expand chevron (expanded) | "Collapse row" | QueueRow expanded state, rightmost column |
| TipTap toolbar: Bold | "Bold" | EditorialEditor toolbar |
| TipTap toolbar: Italic | "Italic" | EditorialEditor toolbar |
| TipTap toolbar: Link | "Insert link" | EditorialEditor toolbar |
| TipTap toolbar: Bullet list | "Bullet list" | EditorialEditor toolbar |
| TipTap toolbar: Ordered list | "Numbered list" | EditorialEditor toolbar |
| TipTap toolbar: Heading H2 | "Heading 2" | EditorialEditor toolbar |
| TipTap toolbar: Heading H3 | "Heading 3" | EditorialEditor toolbar |
| RevisionHistory toggle chevron | "Show change history" / "Hide change history" | EditPanel, below form |

Implementation notes:
- Wrap each icon-only button with `<Tooltip><TooltipTrigger asChild>...</TooltipTrigger><TooltipContent>text</TooltipContent></Tooltip>`.
- Provide a `<TooltipProvider delayDuration={200}>` at the admin layout root to configure open delay once.
- Each icon-only button must also carry `aria-label` matching the tooltip text for screen readers (see Accessibility Contracts).

---

## Interaction Contracts

### Expandable Row

1. Click anywhere on the row (except checkbox) toggles expanded state
2. Chevron rotates 180deg on expand (transition 200ms)
3. Only one row expanded at a time (accordion behavior) -- optional, Claude's discretion
4. Expanded content loads from the same data already fetched (no additional query)
5. Keyboard: Enter/Space on focused row toggles expand

### Bulk Selection

1. Individual checkbox toggles single row selection
2. "Select All" checkbox selects/deselects all visible rows on current page
3. Selection state is local React state (useAdminSelection hook)
4. When count > 0, BulkActionBar slides up from bottom
5. After bulk action completes, clear selection and refresh queue
6. Selection does NOT persist across tab changes

### Edit Panel

1. Click "Edit" button on expanded row opens EditPanel with slide-from-right animation
2. Panel loads instantly with prefetched data (hover prefetch on Edit button)
3. Escape key or overlay click closes panel
4. Unsaved changes: if form is dirty and user tries to close, show "Discard changes?" confirmation (see Unsaved changes prompt in Copywriting)
5. "Save Changes" submits all field updates in a single mutation
6. After save: panel closes, queue row updates reactively (Convex subscription)
7. "Close Panel" button in footer dismisses the panel (triggers dirty-check if form has unsaved changes)

### Approve / Reject

1. Single approve: click "Approve" on expanded row. Immediate status change, row moves to Published tab.
2. Single reject: click "Reject" on expanded row. Confirmation dialog: "Reject [title]? This scholarship will not be visible to students." Two buttons: "Reject" (destructive) / "Keep Scholarship" (neutral).
3. Bulk approve: click "Approve N selected" on BulkActionBar. No confirmation (approve is non-destructive).
4. Bulk reject: click "Reject N selected" on BulkActionBar. Confirmation dialog: "Reject N scholarships? They will not be visible to students." Two buttons: "Reject All" (destructive) / "Keep All" (neutral).
5. Dedup block (ADMN-08): if approving a scholarship whose match_key duplicates an already-published record, show inline error: "Cannot approve: a published scholarship with the same title and organization already exists." with a link to the duplicate.

### Tab Switching

1. Click tab changes the status filter for the queue query
2. Tab counts update in real-time via Convex subscription
3. Active tab: --main bottom border, font-heading
4. Selection clears on tab switch

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | "Approve Scholarship" |
| Bulk approve CTA | "Approve N Selected" |
| Bulk reject CTA | "Reject N Selected" |
| Empty state heading (pending) | "No scholarships pending review" |
| Empty state body (pending) | "All scholarships have been reviewed. New submissions from the scraping pipeline will appear here." |
| Empty state heading (rejected) | "No rejected scholarships" |
| Empty state body (rejected) | "Rejected scholarships will appear here for reference." |
| Empty state heading (all) | "No scholarships found" |
| Empty state body (all) | "Scholarships will appear here after the scraping pipeline runs." |
| Error state | "Failed to load review queue. Check your Convex deployment status and try again." |
| Reject confirmation (single) | "Reject [title]? This scholarship will not be visible to students." |
| Reject confirmation (bulk) | "Reject N scholarships? They will not be visible to students." |
| Reject confirm button (single) | "Reject" |
| Reject cancel button (single) | "Keep Scholarship" |
| Reject confirm button (bulk) | "Reject All" |
| Reject cancel button (bulk) | "Keep All" |
| Dedup block message | "Cannot approve: a published scholarship with the same title and organization already exists." |
| Unsaved changes prompt | "You have unsaved changes. Discard and close?" |
| Unsaved changes confirm button | "Discard Changes" |
| Unsaved changes cancel button | "Continue Editing" |
| Editorial placeholder | "Add tips for applicants..." |
| Trust change confirmation | "Changing trust level to [level] will affect N pending scholarships. Continue?" |
| Select all label | "Select all visible" |
| Bulk bar count | "N scholarships selected" |
| Clear selection | "Clear selection" |
| Save button | "Save Changes" |
| Close panel button | "Close Panel" |
| Desktop-only message | "Admin dashboard requires a desktop browser (1024px minimum)." |
| Stats: total | "Total Scholarships" |
| Stats: pending | "Pending Review" |
| Stats: published today | "Published Today" |
| Stats: source health | "Source Health" |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | Card, Badge, Button (already installed) | not required |

No third-party registries declared.

New dependencies (npm, not registry blocks):
- @tiptap/react 3.20.4
- @tiptap/pm 3.20.4
- @tiptap/starter-kit 3.20.4
- @tiptap/extension-link 3.20.4
- @tiptap/extension-placeholder 3.20.4

Source: RESEARCH.md verified versions via npm view on 2026-03-22.

---

## Accessibility Contracts

| Requirement | Implementation |
|-------------|---------------|
| Expandable rows | `role="row"` with `aria-expanded`, `aria-controls` pointing to expanded content ID |
| Expand/collapse chevron | `aria-label="Expand row"` / `aria-label="Collapse row"` (matches tooltip text, toggles with state) |
| Bulk selection checkboxes | Native `<input type="checkbox">` with visible label, `aria-label="Select [title]"` |
| Select all checkbox | `aria-label="Select all visible scholarships"`, indeterminate state when partial |
| Tabs | Radix Tabs provides keyboard navigation (arrow keys), `aria-selected`, `role="tablist"` |
| EditPanel (Sheet) | Radix Dialog provides focus trap, `aria-modal="true"`, `aria-label="Edit [title]"`, Escape to close |
| EditPanel close button | `aria-label="Close panel"` (matches tooltip text) |
| BulkActionBar | `role="toolbar"`, `aria-label="Bulk actions for N selected scholarships"` |
| TipTap editor | `aria-label="Editorial notes editor"`, toolbar buttons have `aria-pressed` for toggle state |
| TipTap toolbar buttons | Each icon-only button carries `aria-label` matching its tooltip text (e.g., "Bold", "Italic", "Insert link") |
| Status badges | Badge text is sufficient (no additional aria-label needed -- "pending_review" is descriptive) |
| Confirmation dialogs | Radix AlertDialog for reject confirmations with focus on cancel button by default |
| Color contrast | All urgency badge variants meet WCAG AA (4.5:1) against their background -- verified in Phase 6.1 |
| Tooltips | Radix Tooltip with 200ms delay; content accessible to keyboard users via focus trigger |

---

## Dark Mode

All components use CSS custom properties that swap automatically via the `.dark` class. No component needs dark-mode-specific logic.

Admin-specific dark mode notes:
- BulkActionBar inverted colors: uses --foreground bg / --background text. This naturally inverts in dark mode.
- EditPanel shadow: reverse shadow uses --border which adjusts in dark mode.
- Stats card accent border: --main adjusts from oklch(55%) light to oklch(58%) dark.
- TipTap editor: toolbar and content area use --secondary-background and --background which both adapt.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
