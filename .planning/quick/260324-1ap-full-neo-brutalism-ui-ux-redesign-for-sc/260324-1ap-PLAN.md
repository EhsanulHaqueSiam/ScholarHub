---
phase: quick
plan: 260324-1ap
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/index.css
  - web/src/components/ui/button.tsx
  - web/src/components/ui/card.tsx
  - web/src/components/ui/badge.tsx
  - web/src/routes/admin/route.tsx
  - web/src/routes/admin/index.tsx
  - web/src/components/admin/StatCard.tsx
  - web/src/components/admin/StatsBar.tsx
  - web/src/components/admin/ReviewQueue.tsx
  - web/src/components/admin/QueueRow.tsx
  - web/src/components/admin/SourceTrustManager.tsx
  - web/src/components/admin/BulkActionBar.tsx
  - web/src/components/layout/Navbar.tsx
  - web/src/routes/scholarships/index.tsx
  - web/src/components/directory/ScholarshipCard.tsx
  - web/src/components/directory/SearchBar.tsx
  - web/src/components/directory/Pagination.tsx
autonomous: true
must_haves:
  truths:
    - "Neo-brutalism design tokens are bold and authentic: 0px border-radius, 6px shadow offsets, vibrant accent colors"
    - "Admin header has chunky neo-brutalism character with offset shadow and bold branding"
    - "Admin view switcher tabs use pill/button style with borders and shadows instead of thin underlines"
    - "StatCards have bold colored backgrounds, larger typography, and offset shadows"
    - "QueueRow has stronger visual separation, bold hover states with shadow transitions"
    - "SourceTrustManager table has bolder headers, chunkier cell styling"
    - "Public-facing hero has bolder typography with colorful accent on keywords"
    - "SearchBar has stronger neo-brutalism treatment (bigger shadow, thicker border)"
    - "All form inputs use consistent neo-brutalism styling (no native checkboxes)"
  artifacts:
    - path: "web/src/index.css"
      provides: "Updated design tokens with 0px radius, 6px shadows, accent colors, noise texture"
      contains: "--accent"
    - path: "web/src/components/ui/button.tsx"
      provides: "Enhanced button with hover background color shifts"
    - path: "web/src/routes/admin/route.tsx"
      provides: "Bold admin header with shadow and branding"
    - path: "web/src/components/admin/StatCard.tsx"
      provides: "Bold stat cards with colored backgrounds and offset shadows"
  key_links:
    - from: "web/src/index.css"
      to: "all components"
      via: "CSS custom properties"
      pattern: "--border-radius|--box-shadow|--accent"
---

<objective>
Full neo-brutalism UI/UX redesign for ScholarHub — strengthen design tokens to authentic neo-brutalism (hard corners, bold shadows, vibrant accents), overhaul admin dashboard components for visual impact, and polish public-facing directory UI.

Purpose: The current implementation uses neo-brutalism-inspired design but is too soft/timid. Authentic neo-brutalism demands hard edges (0px radius), bold shadow offsets (6-8px), vibrant accent colors, and strong visual contrast. This redesign makes the aesthetic consistent and impactful across admin and public UIs.

Output: Updated design tokens, base component library, admin dashboard, and public directory — all with cohesive bold neo-brutalism aesthetic.
</objective>

<context>
@web/src/index.css
@web/src/components/ui/button.tsx
@web/src/components/ui/card.tsx
@web/src/components/ui/badge.tsx
@web/src/routes/admin/route.tsx
@web/src/routes/admin/index.tsx
@web/src/components/admin/StatCard.tsx
@web/src/components/admin/StatsBar.tsx
@web/src/components/admin/ReviewQueue.tsx
@web/src/components/admin/QueueRow.tsx
@web/src/components/admin/SourceTrustManager.tsx
@web/src/components/admin/BulkActionBar.tsx
@web/src/components/layout/Navbar.tsx
@web/src/routes/scholarships/index.tsx
@web/src/components/directory/ScholarshipCard.tsx
@web/src/components/directory/SearchBar.tsx
@web/src/components/directory/Pagination.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strengthen design tokens and base UI components</name>
  <files>
    web/src/index.css
    web/src/components/ui/button.tsx
    web/src/components/ui/card.tsx
    web/src/components/ui/badge.tsx
  </files>
  <action>
**index.css — Design Token Overhaul:**

1. Change `--border-radius: 5px` to `--border-radius: 0px` for authentic hard-corner neo-brutalism.

2. Increase shadow offsets from 4px to 6px:
   - `--box-shadow-x: 6px`
   - `--box-shadow-y: 6px`
   - `--reverse-box-shadow-x: -6px`
   - `--reverse-box-shadow-y: -6px`

3. Add vibrant accent color palette (light mode) after `--main`:
   ```css
   --accent: oklch(82% 0.19 90);          /* bright yellow */
   --accent-foreground: oklch(0% 0 0);
   --accent-pink: oklch(68% 0.22 350);    /* hot pink */
   --accent-lime: oklch(85% 0.22 140);    /* electric lime */
   --accent-sky: oklch(75% 0.15 230);     /* bright sky blue */
   ```

4. Add dark mode accent variants inside `.dark`:
   ```css
   --accent: oklch(78% 0.17 90);
   --accent-foreground: oklch(0% 0 0);
   --accent-pink: oklch(65% 0.20 350);
   --accent-lime: oklch(80% 0.20 140);
   --accent-sky: oklch(70% 0.14 230);
   ```

5. Register accent colors in `@theme inline`:
   ```css
   --color-accent: var(--accent);
   --color-accent-foreground: var(--accent-foreground);
   --color-accent-pink: var(--accent-pink);
   --color-accent-lime: var(--accent-lime);
   --color-accent-sky: var(--accent-sky);
   ```

6. Make grid background dots more visible — increase dot radius from r='1.5' to r='2' and use a bolder color. Light mode: `%23b0a898` (warmer/darker). Dark mode: `%23ffffff28` (more opaque).

7. Add a subtle noise texture utility class after `.dark .grid-bg`:
   ```css
   .noise-texture::before {
     content: '';
     position: fixed;
     inset: 0;
     opacity: 0.03;
     pointer-events: none;
     z-index: 9999;
     background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)'/%3E%3C/svg%3E");
   }
   ```

**button.tsx — Enhanced Button Variants:**

1. Add `font-heading font-bold` to base cva classes (after `text-sm font-base`), replacing `font-base` with `font-heading`.

2. Update `default` variant: add `hover:brightness-110` for a subtle brightness boost on hover alongside the existing shadow translation.

3. Add a new `accent` variant:
   ```
   "text-accent-foreground bg-accent border-2 border-border shadow-shadow hover:translate-x-boxShadowX hover:translate-y-boxShadowY hover:shadow-none"
   ```

4. Update `neutral` variant hover: add `hover:bg-background` so there is a visible background change on hover (not just shadow shift).

**card.tsx — Bolder Card:**

1. The base cva already uses `shadow-shadow border-2` which will automatically pick up the stronger 6px shadow from the token change. No structural changes needed beyond verifying it looks right with 0px radius.

**badge.tsx — Improved Readability:**

1. Change base badge text from `text-xs` to `text-[13px]` for slightly better readability while staying compact.

2. Add an `accent` variant:
   ```
   "bg-accent text-accent-foreground border-accent-foreground/20"
   ```
  </action>
  <verify>
    <automated>cd /home/siam/Personal/ScholarHub/web && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - Border radius is 0px (hard corners everywhere)
    - Shadow offsets are 6px (bold, visible shadows)
    - 4 accent color CSS variables exist in both light and dark mode
    - Accent colors registered in Tailwind theme
    - Grid dots are larger and more visible
    - Noise texture utility class exists
    - Button has font-heading, hover brightness, and accent variant
    - Badge has improved text size and accent variant
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Admin dashboard neo-brutalism overhaul</name>
  <files>
    web/src/routes/admin/route.tsx
    web/src/routes/admin/index.tsx
    web/src/components/admin/StatCard.tsx
    web/src/components/admin/StatsBar.tsx
    web/src/components/admin/ReviewQueue.tsx
    web/src/components/admin/QueueRow.tsx
    web/src/components/admin/SourceTrustManager.tsx
    web/src/components/admin/BulkActionBar.tsx
  </files>
  <action>
**AdminHeader (route.tsx):**

Replace the thin 48px header with a bold neo-brutalism header:
- Height: `h-16` (from h-12)
- Add bottom shadow: `shadow-[0_6px_0_0_var(--border)]` instead of just `border-b-2`
- Background: keep `bg-secondary-background`
- Add `border-b-4 border-border` (thicker border)
- Title "ScholarHub Admin" should use `text-xl font-heading` with a colored accent badge or underline. Add a small `<span className="text-accent">Admin</span>` to make "Admin" pop in the accent yellow color.
- Give the overall div a `grid-bg` class for the dotted background texture behind the header content.
- The theme toggle button is fine as-is.

**Admin View Switcher Tabs (index.tsx):**

Replace the thin underline tab buttons with bold neo-brutalism pill buttons:
- Change container from `flex gap-4 border-b-2 border-border` to `flex gap-3 flex-wrap`
- Each tab button: `px-4 py-2 text-sm font-heading border-2 border-border rounded-base transition-all`
- Active state: `bg-main text-main-foreground shadow-shadow` (filled, with shadow)
- Inactive state: `bg-secondary-background text-foreground hover:translate-x-[2px] hover:translate-y-[2px] hover:shadow-none shadow-shadow`
- Remove the `-mb-[2px]` offset hack and `border-b-2 border-main` active style entirely.

**StatCard.tsx — Bold Visual Impact:**

Replace the current subtle left-border card with a full neo-brutalism stat card:
- Remove the Card component wrapper. Use a raw div instead for full control.
- Structure: `<div className="border-2 border-border bg-accent/10 shadow-shadow p-6 rounded-base">` (using the new accent tint as background)
- Icon: `size-10 text-main` (bigger, from size-8)
- Value: `text-3xl font-heading` (bigger, from text-2xl)
- Label: `text-sm text-foreground/70 font-heading uppercase tracking-wide` (was text-xs font-base, now bolder)
- Add a colored top border: `border-t-4 border-main` for visual anchoring.
- The entire card should have the standard shadow (`shadow-shadow`).

**StatsBar.tsx:**

- Update skeleton placeholders to match: `h-28` (was h-24) with same border/shadow styling.
- The grid layout (`grid-cols-4 gap-8`) can stay but reduce gap to `gap-6` to keep cards visible.

**ReviewQueue.tsx — Bold Tab Treatment:**

The Tabs.List and Tabs.Trigger inside ReviewQueue also use the thin underline style. Update:
- `Tabs.List`: change from `flex gap-4 border-b-2 border-border mb-4` to `flex gap-3 flex-wrap mb-6`
- `Tabs.Trigger`: same pill-button pattern as the admin view switcher above.
- Active: `bg-main text-main-foreground shadow-shadow border-2 border-border px-4 py-2 rounded-base font-heading text-sm`
- Inactive: `bg-secondary-background text-foreground border-2 border-border px-4 py-2 rounded-base font-heading text-sm shadow-shadow hover:translate-x-[2px] hover:translate-y-[2px] hover:shadow-none`
- Remove the `-mb-[2px]` and `border-b-2 border-main` active styling.

Also update the "Select all" checkbox area:
- Replace `<input type="checkbox"` with a styled div-based checkbox or at minimum add: `className="size-5 accent-main cursor-pointer appearance-none border-2 border-border rounded-none bg-secondary-background checked:bg-main checked:border-main relative"` and a checkmark pseudo-element via a wrapper. Simplest approach: keep native checkbox but wrap in a label with `[&_input]:accent-main` and ensure the `size-5` is visually bold enough. Keep the native checkbox but add `rounded-none` to match hard corners.

**QueueRow.tsx — Stronger Visual Separation:**

1. Outer container: change `border-b border-border` to `border-2 border-border mb-2 rounded-base overflow-hidden` so each row is a distinct card-like block instead of a flat list.

2. Compact row: change hover from `hover:-translate-y-px hover:shadow-sm` to `hover:-translate-y-0.5 hover:shadow-shadow` for a more dramatic neo-brutalism hover effect. Add `transition-all duration-150`.

3. Selected state: change `bg-main/5` to `bg-accent/10 border-l-4 border-l-main` for a bolder selection indicator.

4. Checkbox: Add `rounded-none` to the input to match hard corners aesthetic.

5. Expanded content: change `border-t border-border` to `border-t-2 border-border bg-background` (slightly different background to distinguish from collapsed state).

6. Expand chevron button: give it `border-2 border-border rounded-base hover:bg-main/10` to make it look like a proper neo-brutalism button.

**SourceTrustManager.tsx — Bolder Table:**

1. Table header row: change `py-3` to `py-4` and add `font-bold`. Make header text `text-xs` to `text-sm` and keep uppercase tracking.

2. Table rows: change `py-3 border-b border-border` to `py-4 border-b-2 border-border`. On hover add a bg shift: wrap with a class that does `hover:bg-accent/5`.

3. The select dropdown: add `font-heading` and change `rounded-base` to ensure it uses the 0px border-radius from token. Increase padding slightly: `px-3 py-1.5`.

4. Active/inactive visual: keep the opacity-50 for inactive but also add a subtle strikethrough or `grayscale` filter: `opacity-50 grayscale`.

**BulkActionBar.tsx — More Dramatic:**

1. Main bar: change `h-14` to `h-16` for more presence.
2. Replace `shadow-[0_-4px_0_0_var(--border)]` with `shadow-[0_-6px_0_0_var(--border)]` to match the new 6px token.
3. Add `border-t-4 border-border` (thicker top border, was border-t-2).
4. The tag dropdown popup: ensure it uses the new 0px border-radius from token (it already uses `rounded-base` which will pick it up).
  </action>
  <verify>
    <automated>cd /home/siam/Personal/ScholarHub/web && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - Admin header is 64px tall with 4px bottom border, offset shadow, and accent-colored "Admin" text
    - Admin view switcher uses pill-button tabs with borders and shadows (no underline tabs)
    - StatCards have colored backgrounds, top border accent, larger typography (3xl value, sm label)
    - QueueRows are distinct card-like blocks with bold hover states and clear selection indicators
    - ReviewQueue tabs use pill-button style matching admin view switcher
    - SourceTrustManager table has bolder headers, chunkier rows, hover states
    - BulkActionBar is taller with 6px top shadow and thicker border
    - All checkboxes have rounded-none for hard corners
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Public-facing directory neo-brutalism polish</name>
  <files>
    web/src/components/layout/Navbar.tsx
    web/src/routes/scholarships/index.tsx
    web/src/components/directory/ScholarshipCard.tsx
    web/src/components/directory/SearchBar.tsx
    web/src/components/directory/Pagination.tsx
  </files>
  <action>
**Navbar.tsx:**

1. The navbar already has a `border-b-4` which is good. With the 0px radius token update, all nav links will automatically get hard corners.
2. Add a subtle `shadow-[0_4px_0_0_var(--border)]` to the navbar for a bold bottom shadow effect (replace or add to the existing border-b-4). Actually: keep `border-b-4 border-border` and add `shadow-[0_6px_0_0_var(--border)]` for a double-line effect.
3. Logo hover: the current hover adds shadow and translates — this is good and will be bolder with 6px tokens automatically.

**Hero Section (scholarships/index.tsx):**

1. Title: Change `text-[32px] md:text-[48px]` to `text-[36px] md:text-[56px]` for more dramatic sizing.

2. Add a colorful accent to key words. Wrap "International" in `<span className="text-accent-pink">International</span>` and "Scholarships" in `<span className="text-accent">Scholarships</span>` (yellow accent) to add the vibrant color pops neo-brutalism is known for.

3. The subtitle paragraph: keep as-is, the muted text provides good contrast against the bold heading.

4. Results count badge (the `<p>` element in the sort row): change from `border-2 border-border rounded-base px-3 py-1.5 bg-secondary-background` to `border-2 border-border rounded-base px-4 py-2 bg-accent text-accent-foreground font-heading shadow-shadow` — make it pop with the accent yellow background to draw attention to the count.

**SearchBar.tsx:**

1. Input field: change `border-2` to `border-3` (using arbitrary `border-[3px]`). Note: Tailwind v4 supports `border-3` as utility. If not, use `border-[3px]`.
2. Change `h-12` to `h-14` for a chunkier input.
3. Already has `shadow-shadow` which will pick up the 6px offset. Good.
4. Add `placeholder:text-foreground/40 placeholder:font-heading` for a bolder placeholder.
5. The suggestion dropdown (Popover.Content): already uses `border-2 border-border rounded-base bg-secondary-background shadow-shadow` which is good. Add `border-[3px]` to match the thicker input border.

**ScholarshipCard.tsx:**

1. The hover transition already uses `motion-safe:hover:translate-x-boxShadowX motion-safe:hover:translate-y-boxShadowY hover:shadow-none` which will automatically be bolder with 6px tokens. Good.

2. Add a playful hover scale: After the existing hover classes, add `motion-safe:hover:rotate-[-0.5deg]` for a subtle tilt effect that adds personality. This is a neo-brutalism signature — slight playful rotation on hover.

3. The degree level chips: change `border border-border` to `border-2 border-border bg-secondary-background font-heading` to make them feel more neo-brutalism.

4. Funding amount display: add `font-heading` to the amount span to make it bolder.

**Pagination.tsx:**

1. The page buttons already use `shadow-shadow` and `border-2 border-border` which will pick up the 0px radius and 6px shadows automatically. Good.

2. Add a slight hover rotation to non-current page buttons: `hover:rotate-[-1deg]` for playfulness.

3. Current page button: add `scale-110` to make it slightly larger than its siblings, emphasizing the current position.

4. The "Page X of Y" text: wrap in a neo-brutalism badge style: `border-2 border-border rounded-base px-3 py-1 bg-secondary-background shadow-shadow font-heading` instead of just plain text.
  </action>
  <verify>
    <automated>cd /home/siam/Personal/ScholarHub/web && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - Navbar has bold bottom shadow effect
    - Hero heading uses larger font sizes (36px/56px) with colorful accent words (pink "International", yellow "Scholarships")
    - Results count badge has accent background and shadow
    - SearchBar has thicker 3px border, taller 56px height, bolder placeholder
    - ScholarshipCard has playful hover rotation effect
    - Degree chips have 2px borders and heading font
    - Pagination buttons have playful hover rotation, current page is scaled up
    - Page info text is styled as a neo-brutalism badge
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
After all three tasks complete:

1. `cd web && npx tsc --noEmit` — zero TypeScript errors
2. `cd web && npm run build` — production build succeeds
3. Visual spot-check: all corners are hard (0px radius), shadows are 6px offset, accent colors visible in admin header and hero section
</verification>

<success_criteria>
- Design tokens updated: 0px radius, 6px shadows, 4 accent colors in both light/dark
- Noise texture utility class available
- Grid dots are more visible
- Button has font-heading, accent variant, hover brightness
- Badge has improved text size and accent variant
- Admin header is bold with accent branding and shadow
- Admin tabs (both view switcher and review queue) use pill-button style
- StatCards have colored backgrounds, bold typography, top accent border
- QueueRows are card-like blocks with bold hover and selection states
- SourceTrust table has chunky styling with hover states
- BulkActionBar is taller with matching shadow proportions
- Hero has colorful accent words and larger typography
- SearchBar is chunkier with thicker border
- ScholarshipCard has playful hover effects
- Pagination has neo-brutalism personality
- TypeScript compiles, production build succeeds
</success_criteria>

<output>
After completion, create `.planning/quick/260324-1ap-full-neo-brutalism-ui-ux-redesign-for-sc/260324-1ap-SUMMARY.md`
</output>
