---
phase: 03-scraping-pipeline
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - scraping/src/scholarhub_pipeline/configs/agg_scholars4dev.py
  - scraping/src/scholarhub_pipeline/configs/agg_findaphd.py
autonomous: true
gap_closure: true
requirements: [SCRP-01, SCRP-02]

must_haves:
  truths:
    - "At least 2 source configs use primary_method='rss' so the RssScraper code path is exercised in production"
    - "RSS configs specify feed_url in selectors pointing to actual RSS/Atom feed endpoints"
  artifacts:
    - path: "scraping/src/scholarhub_pipeline/configs/agg_scholars4dev.py"
      provides: "Scholars4Dev config with primary_method='rss'"
      contains: "primary_method"
    - path: "scraping/src/scholarhub_pipeline/configs/agg_findaphd.py"
      provides: "FindAPhD config with primary_method='rss'"
      contains: "primary_method"
  key_links:
    - from: "scraping/src/scholarhub_pipeline/configs/agg_scholars4dev.py"
      to: "scraping/src/scholarhub_pipeline/scrapers/rss_scraper.py"
      via: "primary_method='rss' triggers get_scraper to return RssScraper"
      pattern: "primary_method.*=.*rss"
    - from: "scraping/src/scholarhub_pipeline/configs/agg_findaphd.py"
      to: "scraping/src/scholarhub_pipeline/scrapers/rss_scraper.py"
      via: "primary_method='rss' triggers get_scraper to return RssScraper"
      pattern: "primary_method.*=.*rss"
---

<objective>
Assign primary_method='rss' to 2 source configs that have known RSS/Atom feeds.

Purpose: Close verification gap 3 -- the RssScraper is implemented and tested but zero configs use it, making it dead code in production. Scholars4Dev and FindAPhD both publish RSS feeds that should be used as the primary scraping method.

Output: Two config files updated with primary_method='rss', appropriate feed URLs in selectors, and HTML scraping demoted to secondary_method.
</objective>

<execution_context>
@/home/siam/.claude/get-shit-done/workflows/execute-plan.md
@/home/siam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-scraping-pipeline/03-VERIFICATION.md
@.planning/phases/03-scraping-pipeline/03-06-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From scraping/src/scholarhub_pipeline/configs/_protocol.py:
```python
@runtime_checkable
class SourceConfig(Protocol):
    name: str
    url: str
    source_id: str
    primary_method: str       # "api" | "jsonld" | "ajax" | "rss" | "scrape" | "scrapling"
    secondary_method: str | None
    selectors: dict[str, str]
    field_mappings: dict[str, str]
    pagination: dict | None
    detail_page: bool
    detail_selectors: dict[str, str] | None
    auth_config: dict | None
    rate_limit_delay: float
    cutoff_months: int
```

From scraping/src/scholarhub_pipeline/configs/_bases.py:
```python
@dataclass
class BaseAggregatorConfig(BaseSourceConfig):
    rate_limit_delay: float = 1.5
```

From scraping/src/scholarhub_pipeline/scrapers/rss_scraper.py (how feed_url is consumed):
The RssScraper reads `config.url` as the feed URL when primary_method is 'rss'. It also checks `config.selectors` for a `feed_url` key as an override. The selectors dict should include `feed_url` pointing to the actual RSS endpoint if it differs from the main `config.url`.

From scraping/src/scholarhub_pipeline/scrapers/__init__.py (factory):
The get_scraper() factory maps primary_method='rss' to RssScraper.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Scholars4Dev and FindAPhD configs to use RSS as primary method</name>
  <files>scraping/src/scholarhub_pipeline/configs/agg_scholars4dev.py, scraping/src/scholarhub_pipeline/configs/agg_findaphd.py</files>
  <read_first>
    - scraping/src/scholarhub_pipeline/configs/agg_scholars4dev.py
    - scraping/src/scholarhub_pipeline/configs/agg_findaphd.py
    - scraping/src/scholarhub_pipeline/scrapers/rss_scraper.py
    - scraping/src/scholarhub_pipeline/scrapers/__init__.py
    - scraping/src/scholarhub_pipeline/configs/_bases.py
    - scraping/src/scholarhub_pipeline/configs/_protocol.py
  </read_first>
  <action>
**Update agg_scholars4dev.py:**

Change the Config dataclass fields:
- `primary_method: str = "rss"` (was "scrape")
- `secondary_method: str | None = "scrape"` (was "scrapling")
- Add `feed_url` key to selectors dict: `'feed_url': 'https://www.scholars4dev.com/feed/'`
- Keep all existing selectors (they are used by the secondary_method fallback when HTML scraping is needed)
- Set `detail_page: bool = True` (keep as-is, RSS entries often need detail page follow for full description)
- Keep all existing detail_selectors, field_mappings, and pagination as-is (used by fallback)

The Scholars4Dev WordPress site publishes a standard RSS 2.0 feed at `https://www.scholars4dev.com/feed/` containing scholarship post titles, links, publication dates, and excerpts.

**Update agg_findaphd.py:**

Change the Config dataclass fields:
- `primary_method: str = "rss"` (was "scrape")
- `secondary_method: str | None = "scrape"` (was "scrapling")
- Add `feed_url` key to selectors dict: `'feed_url': 'https://www.findaphd.com/phds/rss'`
- Keep all existing selectors (used by secondary_method fallback)
- Set `detail_page: bool = True` (keep as-is)
- Keep all existing detail_selectors, field_mappings, and pagination as-is (used by fallback)

FindAPhD publishes an RSS feed at `https://www.findaphd.com/phds/rss` containing PhD funding opportunities.

Both configs retain all existing HTML selectors intact because they serve as the fallback scrape method. The only changes are: primary_method rss, secondary_method scrape, and feed_url added to selectors.
  </action>
  <verify>
    <automated>cd /home/siam/Personal/ScholarHub/scraping && python -c "from scholarhub_pipeline.configs.agg_scholars4dev import CONFIG as c1; from scholarhub_pipeline.configs.agg_findaphd import CONFIG as c2; assert c1.primary_method == 'rss', f'scholars4dev: {c1.primary_method}'; assert c2.primary_method == 'rss', f'findaphd: {c2.primary_method}'; assert 'feed_url' in c1.selectors; assert 'feed_url' in c2.selectors; print('OK: both configs use rss with feed_url')"</automated>
  </verify>
  <acceptance_criteria>
    - agg_scholars4dev.py contains `primary_method: str = "rss"`
    - agg_scholars4dev.py contains `secondary_method: str | None = "scrape"`
    - agg_scholars4dev.py selectors dict contains key `feed_url` with value `https://www.scholars4dev.com/feed/`
    - agg_findaphd.py contains `primary_method: str = "rss"`
    - agg_findaphd.py contains `secondary_method: str | None = "scrape"`
    - agg_findaphd.py selectors dict contains key `feed_url` with value `https://www.findaphd.com/phds/rss`
    - Both configs still pass the SourceConfig protocol check: `python -m pytest tests/test_configs/test_config_protocol.py -x -q` passes
    - `python -c "from scholarhub_pipeline.configs import discover_configs; configs = discover_configs(); rss = [c for c in configs if c.primary_method == 'rss']; assert len(rss) >= 2"` exits 0
  </acceptance_criteria>
  <done>Two source configs (Scholars4Dev and FindAPhD) use primary_method='rss' with feed_url selectors pointing to real RSS endpoints. The RssScraper code path is now exercisable in production. Secondary method is 'scrape' for fallback.</done>
</task>

</tasks>

<verification>
1. `grep -c "primary_method.*rss" scraping/src/scholarhub_pipeline/configs/agg_scholars4dev.py scraping/src/scholarhub_pipeline/configs/agg_findaphd.py` -- both files show 1
2. `cd scraping && python -c "from scholarhub_pipeline.configs import discover_configs; rss = [c for c in discover_configs() if c.primary_method == 'rss']; print(f'{len(rss)} RSS configs: {[c.name for c in rss]}')"` -- shows 2 RSS configs
3. `cd scraping && python -m pytest tests/test_configs/test_config_protocol.py -x -q` -- all protocol tests pass
</verification>

<success_criteria>
- At least 2 source configs have primary_method='rss'
- Each RSS config has a feed_url selector pointing to a real RSS endpoint URL
- All existing config protocol validation tests pass
- RssScraper would be instantiated by get_scraper() when these configs are processed
</success_criteria>

<output>
After completion, create `.planning/phases/03-scraping-pipeline/03-09-SUMMARY.md`
</output>
