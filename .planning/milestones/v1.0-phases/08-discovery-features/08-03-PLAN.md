---
phase: 08-discovery-features
plan: 03
type: execute
wave: 2
depends_on: [08-01, 08-02]
files_modified:
  - web/src/components/admin/CollectionsManager.tsx
  - web/src/components/admin/CollectionEditForm.tsx
  - web/src/components/admin/TagsManager.tsx
  - web/src/components/admin/SuggestedTagReview.tsx
  - web/src/components/admin/EditForm.tsx
  - web/src/components/admin/BulkActionBar.tsx
  - web/src/routes/admin/index.tsx
autonomous: true
requirements: [DISC-01]

must_haves:
  truths:
    - "Admin can see Collections and Tags tabs in the admin dashboard"
    - "Admin can create, edit, and archive collections via slide-out form"
    - "Admin can manage tags: view grouped list, review suggested tags, bulk-tag scholarships"
    - "Collection edit form shows live preview with matching scholarship count"
    - "Admin can add/remove tags on individual scholarships via the EditForm slide-out (D-34)"
    - "Admin can bulk-tag selected scholarships from the review queue via BulkActionBar (D-36)"
  artifacts:
    - path: "web/src/components/admin/CollectionsManager.tsx"
      provides: "Collections tab with table list and CRUD"
      min_lines: 100
    - path: "web/src/components/admin/CollectionEditForm.tsx"
      provides: "Collection create/edit slide-out form with live preview"
      min_lines: 100
    - path: "web/src/components/admin/TagsManager.tsx"
      provides: "Tags tab with grouped list, pending review, filter-then-tag"
      min_lines: 100
    - path: "web/src/components/admin/SuggestedTagReview.tsx"
      provides: "Accept/reject suggested tags with reason tooltip"
      min_lines: 30
    - path: "web/src/components/admin/EditForm.tsx"
      provides: "Scholarship edit form with tag multi-select field"
      contains: "addTagToScholarship"
    - path: "web/src/components/admin/BulkActionBar.tsx"
      provides: "Bulk action bar with Tag Selected button"
      contains: "Tag Selected"
    - path: "web/src/routes/admin/index.tsx"
      provides: "Admin dashboard with 4 tabs: queue, sources, collections, tags"
      contains: "Collections"
  key_links:
    - from: "web/src/routes/admin/index.tsx"
      to: "web/src/components/admin/CollectionsManager.tsx"
      via: "React state tab switching renders CollectionsManager"
      pattern: "CollectionsManager"
    - from: "web/src/components/admin/CollectionEditForm.tsx"
      to: "web/convex/collections.ts"
      via: "useMutation for createCollection/updateCollection"
      pattern: "api.collections"
    - from: "web/src/components/admin/EditForm.tsx"
      to: "web/convex/tags.ts"
      via: "useMutation for addTagToScholarship/removeTag"
      pattern: "api.tags"
    - from: "web/src/components/admin/BulkActionBar.tsx"
      to: "web/convex/tags.ts"
      via: "useMutation for bulkAddTags"
      pattern: "api.tags.bulkAddTags"
---

<objective>
Build the admin UI for collection and tag management: Collections tab (table + slide-out edit form with live preview), Tags tab (grouped tag list, suggested tag review, filter-then-tag bulk operations). Also extend the existing EditForm with per-scholarship tag editing and the BulkActionBar with a "Tag Selected" bulk action. Extends the existing admin dashboard tab switcher.

Purpose: Admin needs tools to create curated collections and manage the tag system before public-facing UI can display collections and tags. Per-scholarship tag editing (D-34) and review queue bulk tagging (D-36) are essential for day-to-day tag management.
Output: 4 new admin components + 2 extended admin components + updated admin route with 4 tabs.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-discovery-features/08-CONTEXT.md
@.planning/phases/08-discovery-features/08-UI-SPEC.md

@web/src/routes/admin/index.tsx
@web/src/components/admin/EditPanel.tsx
@web/src/components/admin/EditForm.tsx
@web/src/components/admin/ReviewQueue.tsx
@web/src/components/admin/SourceTrustManager.tsx
@web/src/components/admin/BulkActionBar.tsx
@web/convex/collections.ts
@web/convex/tags.ts

<interfaces>
From web/src/routes/admin/index.tsx:
```typescript
// Admin view switcher: queue or sources
const [adminView, setAdminView] = useState<"queue" | "sources">("queue");
// Tab buttons with border-b-2 border-main active state
```

From web/src/components/admin/EditPanel.tsx:
```typescript
// Uses Radix Dialog as slide-out sheet
import * as Dialog from "@radix-ui/react-dialog";
// Props: open, scholarshipId, scholarshipTitle, onClose
```

From web/src/components/admin/EditForm.tsx:
```typescript
interface EditFormProps {
  scholarshipId: Id<"scholarships">;
  onSaved: () => void;
  onDirtyChange?: (dirty: boolean) => void;
}
// Form with controlled state for all scholarship fields
// DEGREE_OPTIONS, FUNDING_TYPE_OPTIONS, STATUS_OPTIONS arrays
```

From web/src/components/admin/BulkActionBar.tsx:
```typescript
interface BulkActionBarProps {
  selectedCount: number;
  selectedIds: Set<Id<"scholarships">>;
  onClear: () => void;
}
// Fixed bottom bar with bulk Approve/Reject buttons + AlertDialog confirmation
```

From web/convex/collections.ts (created in Plan 01):
```typescript
export const getAdminCollections = query({ ... });
export const createCollection = mutation({ ... });
export const updateCollection = mutation({ ... });
export const deleteCollection = mutation({ ... });
export const bulkUpdateCollectionStatus = mutation({ ... });
export const getCollectionPreview = query({ ... });
```

From web/convex/tags.ts (created in Plan 01):
```typescript
export const getAllTags = query({ ... });
export const getScholarshipTags = query({ ... });
export const addTagToScholarship = mutation({ ... });
export const removeTag = mutation({ ... });
export const renameTag = mutation({ ... });
export const deleteTag = mutation({ ... });
export const bulkAddTags = mutation({ ... });
export const acceptSuggestedTag = mutation({ ... });
export const rejectSuggestedTag = mutation({ ... });
```

From web/src/lib/tags.ts (created in Plan 01):
```typescript
export const TAG_CATEGORIES: { [category: string]: { label: string; tags: Array<{ id: string; label: string; description: string }> } };
export const ALL_TAGS: Array<{ id: string; label: string; description: string }>;
export function getTagLabel(tagId: string): string;
export function getTagDescription(tagId: string): string | undefined;
export function getTagCategory(tagId: string): string | undefined;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CollectionsManager + CollectionEditForm</name>
  <files>
    web/src/components/admin/CollectionsManager.tsx
    web/src/components/admin/CollectionEditForm.tsx
  </files>
  <read_first>
    web/src/components/admin/EditPanel.tsx
    web/src/components/admin/SourceTrustManager.tsx
    web/src/components/admin/BulkActionBar.tsx
    web/src/routes/admin/index.tsx
    web/convex/collections.ts
    .planning/phases/08-discovery-features/08-UI-SPEC.md
    .planning/phases/08-discovery-features/08-CONTEXT.md
  </read_first>
  <action>
**1. Create `web/src/components/admin/CollectionsManager.tsx` (D-81, D-82):**

A table-based list of all collections (any status) with CRUD actions.

Structure:
- "New Collection" button (default variant) at top
- Table with columns: Name (emoji prefix + name), Status (Badge: draft/active/archived with neutral variant), Scholarships (cached count number), Sort Order (inline numeric input, 0-999, saves on blur via updateCollection mutation), Actions (Edit button, Archive/Activate button, Preview link opens /collections/{slug} in new tab)
- Row selection checkboxes for bulk operations
- BulkActionBar at bottom when items selected: "Archive Selected" / "Activate Selected" buttons

Use `useQuery(api.collections.getAdminCollections)` for data.
Use `useMutation(api.collections.updateCollection)` for inline sort order saves.
Use `useMutation(api.collections.bulkUpdateCollectionStatus)` for bulk operations.
Use `useMutation(api.collections.deleteCollection)` for delete with AlertDialog confirmation.

State: `editingCollection` (Id or null) for slide-out form, `selectedIds` (Set) for bulk selection.

Sort order input: `<input type="number" min={0} max={999} className="w-16 border-2 border-border rounded-base px-2 py-1 text-sm" defaultValue={collection.sort_order} onBlur={(e) => updateCollection({ collectionId: collection._id, updates: { sort_order: parseInt(e.target.value) || 0 } })} />` (D-84: numeric input over drag-and-drop).

Archive confirmation uses Radix AlertDialog (same pattern as BulkActionBar). Copy text: "Archive this collection? It will be hidden from public view but can be reactivated later." Buttons: "Keep Collection" / "Archive Collection" (destructive variant) per copywriting contract.

Render the CollectionEditForm in a Radix Dialog sheet (same slide-out pattern as EditPanel.tsx: `Dialog.Root` + `Dialog.Portal` + `Dialog.Overlay` + `Dialog.Content` with `data-[state=open]:animate-in` classes).

**2. Create `web/src/components/admin/CollectionEditForm.tsx` (D-82, D-83, D-16):**

A form for creating/editing collections inside the slide-out sheet.

Fields (all controlled state):
- Name: text input
- Slug: text input, auto-generated from name on name change (only if creating new or slug hasn't been manually edited). Display generated slug below name input in muted text. (D-05)
- Emoji: text input (single character/emoji), with preview
- Description: textarea for markdown (D-10)
- Status: select with options draft/active/archived
- Featured: checkbox toggle (D-07)
- Default Sort: select with options "deadline" / "prestige" / "newest" (D-11)
- Sort Order: number input (D-09)

Filter Criteria section (collapsible):
- Host Countries: multi-select text input (comma-separated country codes)
- Degree Levels: checkbox group (bachelor, master, phd, postdoc)
- Funding Types: checkbox group (fully_funded, partial, tuition_waiver, stipend_only)
- Prestige Tiers: checkbox group (gold, silver, bronze)
- Tags: multi-select text input with autocomplete from TAG_CATEGORIES
- Deadline Before / After: date inputs
- Added Since: date input

Live Preview section at bottom (D-16, D-83):
- Use `useQuery(api.collections.getCollectionPreview, filterCriteria)` with debounced filter criteria (300ms debounce)
- Show "Matching: {N} scholarships" count
- Show first 5 scholarship titles as a compact list

Save button calls `createCollection` or `updateCollection` mutation depending on whether `collection` prop is provided.
Cancel button calls `onCancel` prop.

Props: `{ collection?: CollectionDoc; onSave: () => void; onCancel: () => void }`.
  </action>
  <verify>
    <automated>grep -c "CollectionsManager" /home/siam/Personal/ScholarHub/web/src/components/admin/CollectionsManager.tsx && grep -c "CollectionEditForm" /home/siam/Personal/ScholarHub/web/src/components/admin/CollectionEditForm.tsx && grep -c "getCollectionPreview" /home/siam/Personal/ScholarHub/web/src/components/admin/CollectionEditForm.tsx</automated>
  </verify>
  <acceptance_criteria>
    - web/src/components/admin/CollectionsManager.tsx contains `export` and `CollectionsManager`
    - web/src/components/admin/CollectionsManager.tsx contains `api.collections.getAdminCollections`
    - web/src/components/admin/CollectionsManager.tsx contains `api.collections.updateCollection`
    - web/src/components/admin/CollectionsManager.tsx contains `Sort Order` or `sort_order`
    - web/src/components/admin/CollectionsManager.tsx contains `Archive` and `AlertDialog`
    - web/src/components/admin/CollectionEditForm.tsx contains `export` and `CollectionEditForm`
    - web/src/components/admin/CollectionEditForm.tsx contains `api.collections.getCollectionPreview`
    - web/src/components/admin/CollectionEditForm.tsx contains `api.collections.createCollection` or `api.collections.updateCollection`
    - web/src/components/admin/CollectionEditForm.tsx contains `Matching`
  </acceptance_criteria>
  <done>CollectionsManager shows table of all collections with inline sort order editing, bulk archive/activate, and slide-out create/edit form. CollectionEditForm has all fields including filter criteria with live preview showing matching scholarship count.</done>
</task>

<task type="auto">
  <name>Task 2: TagsManager + SuggestedTagReview + EditForm tags + BulkActionBar tags + admin route wiring</name>
  <files>
    web/src/components/admin/TagsManager.tsx
    web/src/components/admin/SuggestedTagReview.tsx
    web/src/components/admin/EditForm.tsx
    web/src/components/admin/BulkActionBar.tsx
    web/src/routes/admin/index.tsx
  </files>
  <read_first>
    web/src/routes/admin/index.tsx
    web/src/components/admin/ReviewQueue.tsx
    web/src/components/admin/EditForm.tsx
    web/src/components/admin/BulkActionBar.tsx
    web/src/lib/tags.ts
    web/convex/tags.ts
    .planning/phases/08-discovery-features/08-UI-SPEC.md
    .planning/phases/08-discovery-features/08-CONTEXT.md
  </read_first>
  <action>
**1. Create `web/src/components/admin/TagsManager.tsx` (D-34 through D-39):**

Three sections layout:

**Section 1: Grouped Tag List (D-38)**
- Import TAG_CATEGORIES from `@/lib/tags`
- Collapsible category sections (5 categories) using details/summary HTML or state-controlled
- Each tag row: tag label (Badge with `tag` variant), scholarship count (from `getAllTags` query which returns counts), rename button (opens inline text input), delete button with AlertDialog confirmation showing impact: "Delete tag '{label}'? This tag is used by {N} scholarships and {M} collections. It will be removed from all of them." Buttons: "Keep Tag" / "Delete Tag" (destructive). Per copywriting contract.
- Inline creation at bottom of each category: text input + Enter to create. Auto-generate snake_case from display name (D-35). Uses `addTag` mutation from tags.ts.
- Freeform tags (not in any predefined category) shown in a "Custom" section

**Section 2: Pending Suggested Tags (D-38)**
- Query scholarships with non-empty suggested_tags field
- Table rows: scholarship title, suggested tag count
- Expandable row (same pattern as QueueRow.tsx) showing SuggestedTagReview component
- "Review All" link to scroll to this section

**Section 3: Filter-Then-Tag Bulk Interface (D-36, D-38)**
- Search input for filtering scholarships (uses searchSuggestions or simple text filter)
- "Show only untagged" toggle (D-39 untagged filter)
- Results list showing scholarship titles with their current tags
- Multi-select tag input at top: select tags to apply
- "Apply {selected tags} to {N} scholarships" button with confirmation count
- Uses bulkAddTags mutation from tags.ts

**2. Create `web/src/components/admin/SuggestedTagReview.tsx` (D-37):**

Props: `{ scholarshipId: Id<"scholarships">; suggestedTags: Array<{ tag: string; reason: string; suggested_at: number }> }`.

Renders each suggested tag as a Badge with `tagSuggested` variant (amber). Each has:
- Accept button: Check icon (lucide-react), calls acceptSuggestedTag mutation (moves tag from suggested_tags to tags)
- Reject button: X icon (lucide-react), calls rejectSuggestedTag mutation (removes from suggested_tags)
- Radix Tooltip on hover showing the `reason` (matched text snippet) with 200ms open delay

No confirmation dialog for reject (per copywriting contract: "No confirmation -- immediate action, low-stakes, reversible").

**3. Extend `web/src/components/admin/EditForm.tsx` with tag multi-select field (D-34):**

Read the existing EditForm.tsx carefully first. Add a new "Tags" section after the existing fields (before the Save button area).

Implementation:
- Import `ALL_TAGS` and `getTagLabel` from `@/lib/tags`
- Import `useMutation` calls for `api.tags.addTagToScholarship` and `api.tags.removeTag`
- Query current tags: `useQuery(api.tags.getScholarshipTags, { scholarshipId })` to get both `tags` and `suggested_tags`
- Display current tags as removable Badge pills (tag variant, with X button to remove via `removeTag` mutation)
- Display suggested tags as amber Badge pills with accept/reject buttons (reuse SuggestedTagReview inline or duplicate the accept/reject pattern)
- Add a tag input with autocomplete dropdown: text input that filters ALL_TAGS by typed text, showing matching tag labels in a dropdown. Clicking a tag calls `addTagToScholarship`. Support typing a custom tag name + Enter to add a freeform tag (D-35).
- The tag mutations operate directly on the scholarship (not through the form's save flow) -- they are immediate mutations, not part of the form dirty state. This is intentional: tags are managed independently like the editorial tips editor.

Section label: "Tags" with a `text-sm font-heading` heading.

**4. Extend `web/src/components/admin/BulkActionBar.tsx` with "Tag Selected" button (D-36 partial):**

Read the existing BulkActionBar.tsx carefully first. Add a "Tag Selected" button alongside the existing Approve/Reject buttons.

Implementation:
- Import `useMutation(api.tags.bulkAddTags)` and `ALL_TAGS`/`getTagLabel` from `@/lib/tags`
- Add state: `showTagDropdown` (boolean), `selectedTags` (string[])
- Add a "Tag Selected" Button (neutral variant) that toggles `showTagDropdown`
- When `showTagDropdown` is true, render a dropdown panel above the bar (positioned with `absolute bottom-full`) containing:
  - A compact tag multi-select: list of tag checkboxes (first 15 most common tags from ALL_TAGS), with a search input to filter
  - "Apply {N} tags to {selectedCount} scholarships" button that calls `bulkAddTags({ scholarshipIds: Array.from(selectedIds), tags: selectedTags })`
  - Close button to dismiss dropdown
- The dropdown panel uses same dark theme as the bar: `bg-foreground text-background border-2 border-border rounded-base shadow-shadow p-4`
- After successful bulk tag application, show result message (same pattern as approve/reject) and close dropdown

**5. Update `web/src/routes/admin/index.tsx` (D-81):**

Extend adminView state type from `"queue" | "sources"` to `"queue" | "sources" | "collections" | "tags"`.

Add two new tab buttons after "Source Trust":
- "Collections" tab button (same className pattern as existing tabs)
- "Tags" tab button (same className pattern)

Add conditional rendering:
```tsx
{adminView === "collections" && <CollectionsManager />}
{adminView === "tags" && <TagsManager />}
```

Import CollectionsManager and TagsManager. Keep existing EditPanel and ReviewQueue unchanged.
  </action>
  <verify>
    <automated>grep -c "CollectionsManager\|TagsManager\|collections\|tags" /home/siam/Personal/ScholarHub/web/src/routes/admin/index.tsx && grep -c "SuggestedTagReview" /home/siam/Personal/ScholarHub/web/src/components/admin/SuggestedTagReview.tsx && grep -c "addTagToScholarship\|removeTag\|Tags" /home/siam/Personal/ScholarHub/web/src/components/admin/EditForm.tsx && grep -c "Tag Selected\|bulkAddTags" /home/siam/Personal/ScholarHub/web/src/components/admin/BulkActionBar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - web/src/components/admin/TagsManager.tsx contains `export` and `TagsManager`
    - web/src/components/admin/TagsManager.tsx contains `TAG_CATEGORIES`
    - web/src/components/admin/TagsManager.tsx contains `Delete tag` or `deleteTag`
    - web/src/components/admin/SuggestedTagReview.tsx contains `export` and `SuggestedTagReview`
    - web/src/components/admin/SuggestedTagReview.tsx contains `tagSuggested`
    - web/src/components/admin/SuggestedTagReview.tsx contains `Tooltip`
    - web/src/components/admin/EditForm.tsx contains `addTagToScholarship` or `api.tags.addTagToScholarship`
    - web/src/components/admin/EditForm.tsx contains `removeTag` or `api.tags.removeTag`
    - web/src/components/admin/EditForm.tsx contains `ALL_TAGS` or `getTagLabel`
    - web/src/components/admin/BulkActionBar.tsx contains `Tag Selected`
    - web/src/components/admin/BulkActionBar.tsx contains `bulkAddTags` or `api.tags.bulkAddTags`
    - web/src/routes/admin/index.tsx contains `"queue" | "sources" | "collections" | "tags"`
    - web/src/routes/admin/index.tsx contains `CollectionsManager`
    - web/src/routes/admin/index.tsx contains `TagsManager`
  </acceptance_criteria>
  <done>Admin dashboard has 4 tabs: Review Queue, Source Trust, Collections, Tags. TagsManager shows grouped tag list with counts, pending suggested tag review with amber badges and accept/reject buttons, and filter-then-tag bulk interface. SuggestedTagReview shows matched text snippet tooltips. EditForm has per-scholarship tag multi-select with autocomplete and suggested tag review (D-34). BulkActionBar has "Tag Selected" button with dropdown for quick bulk tagging from the review queue (D-36).</done>
</task>

</tasks>

<verification>
- Admin route has 4 tab buttons: Review Queue, Source Trust, Collections, Tags
- Collections tab shows table with sort order input, archive/activate, slide-out edit form
- Tags tab has 3 sections: grouped list, pending review, filter-then-tag
- EditForm slide-out has Tags section with add/remove/autocomplete (D-34)
- BulkActionBar has "Tag Selected" button with tag dropdown (D-36)
- `cd /home/siam/Personal/ScholarHub/web && npx vitest run --reporter=verbose` -- all tests pass
</verification>

<success_criteria>
Admin can manage collections (CRUD with live preview) and tags (grouped list, suggested tag review, bulk tagging) through the dashboard. Admin can add/remove tags on individual scholarships via the EditForm slide-out (D-34). Admin can bulk-tag selected scholarships from the review queue BulkActionBar (D-36). All admin functionality for DISC-01 is in place.
</success_criteria>

<output>
After completion, create `.planning/phases/08-discovery-features/08-03-SUMMARY.md`
</output>
