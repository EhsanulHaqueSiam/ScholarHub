---
phase: 09-seo-growth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/lib/seo/json-ld.ts
  - web/src/lib/seo/meta.ts
  - web/src/lib/seo/landing-content.ts
  - web/src/lib/seo/sitemap.ts
  - web/convex/seo.ts
  - web/src/routes/__root.tsx
  - web/src/__tests__/seo-jsonld.test.ts
  - web/src/__tests__/seo-meta.test.ts
autonomous: true
requirements:
  - SEOG-01
  - SEOG-02
  - SEOG-03

must_haves:
  truths:
    - "JSON-LD builder functions produce valid Grant, BreadcrumbList, FAQPage, ItemList, and Organization structured data"
    - "Meta tag helpers produce canonical, OG, Twitter Card, and hreflang meta arrays"
    - "Landing content generator produces unique stats, intro paragraphs, FAQ questions, and cross-links per country/degree"
    - "Convex SEO queries return scholarship slugs, country stats, and degree stats"
    - "Root layout includes Organization JSON-LD, default OG tags, hreflang, GA4 script, and GSC verification"
  artifacts:
    - path: "web/src/lib/seo/json-ld.ts"
      provides: "JSON-LD builder functions for all Schema.org types"
      exports: ["buildScholarshipJsonLd", "buildBreadcrumbJsonLd", "buildFaqJsonLd", "buildItemListJsonLd", "buildOrganizationJsonLd"]
    - path: "web/src/lib/seo/meta.ts"
      provides: "Meta tag builder helpers for all page types"
      exports: ["buildCanonicalUrl", "buildOgMeta", "buildPageMeta"]
    - path: "web/src/lib/seo/landing-content.ts"
      provides: "Templated content generation for landing pages"
      exports: ["generateCountryIntro", "generateCountryFaq", "generateDegreeIntro", "generateDegreeFaq", "generateCrossLinks"]
    - path: "web/src/lib/seo/sitemap.ts"
      provides: "Sitemap XML generation utility"
      exports: ["generateSitemapXml"]
    - path: "web/convex/seo.ts"
      provides: "Convex queries for SEO data"
      exports: ["getAllPublishedSlugs", "getCountryStats", "getDegreeStats", "getTopCountries", "getAllDegrees"]
    - path: "web/src/routes/__root.tsx"
      provides: "Root layout with global SEO enhancements"
  key_links:
    - from: "web/src/lib/seo/json-ld.ts"
      to: "web/src/lib/countries.ts"
      via: "getCountryName for place names"
      pattern: "getCountryName"
    - from: "web/src/lib/seo/landing-content.ts"
      to: "web/convex/seo.ts"
      via: "Stats data from Convex queries"
      pattern: "getCountryStats|getDegreeStats"
    - from: "web/src/routes/__root.tsx"
      to: "web/src/lib/seo/json-ld.ts"
      via: "Organization JSON-LD in root"
      pattern: "buildOrganizationJsonLd"
---

<objective>
Create the SEO foundation library: JSON-LD builder functions, meta tag helpers, landing page content generators, sitemap utility, Convex SEO queries, and root-level SEO enhancements.

Purpose: All subsequent plans (route enhancements, OG images, sitemap server route) depend on these shared utilities. Building them first with tests ensures correctness before integration.

Output: `web/src/lib/seo/` module with 4 files, `web/convex/seo.ts` with 5 queries, enhanced `__root.tsx`, and test files for JSON-LD and meta builders.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-seo-growth/09-CONTEXT.md
@.planning/phases/09-seo-growth/09-RESEARCH.md

<interfaces>
<!-- Key types and contracts from existing codebase -->

From web/src/lib/countries.ts:
```typescript
export function getCountryFlag(code: string): string;
export function getCountryName(code: string): string;
export function getCountryCode(name: string): string | undefined;
export function getAllCountries(): Array<{ code: string; name: string }>;
export function parseHostCountries(hostCountry: string): string[];
```

From web/src/lib/country-data.ts:
```typescript
export interface CountryData {
  // ... tuition, visa, intakes, post-study work data
}
export function getCountryData(code: string): CountryData | null;
```

From web/src/lib/shared.ts:
```typescript
export function formatFundingType(type: string): string;
export function formatFundingAmount(scholarship: { ... }): string;
```

From web/convex/schema.ts (scholarship table indexes):
```
.index("by_status", ["status"])
.index("by_status_deadline", ["status", "application_deadline"])
.index("by_status_prestige_deadline", ["status", "prestige_tier", "application_deadline"])
```

From web/src/routes/__root.tsx (current head pattern):
```typescript
export const Route = createRootRouteWithContext<RouterContext>()({
  head: () => ({
    meta: [
      { charSet: "utf-8" },
      { name: "viewport", content: "width=device-width, initial-scale=1.0" },
      { title: "ScholarHub - Find Your Scholarship" },
      { name: "description", content: "Browse 2,400+ international scholarships..." },
    ],
    links: [
      { rel: "stylesheet", href: appCss },
      // ... font preconnects
    ],
  }),
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SEO library -- JSON-LD builders, meta helpers, landing content, sitemap utility, and test scaffolds</name>
  <files>
    web/src/lib/seo/json-ld.ts
    web/src/lib/seo/meta.ts
    web/src/lib/seo/landing-content.ts
    web/src/lib/seo/sitemap.ts
    web/src/__tests__/seo-jsonld.test.ts
    web/src/__tests__/seo-meta.test.ts
  </files>
  <read_first>
    web/src/routes/scholarships/$slug.tsx
    web/src/lib/countries.ts
    web/src/lib/shared.ts
    web/src/lib/country-data.ts
    web/src/routes/__root.tsx
    .planning/phases/09-seo-growth/09-RESEARCH.md
  </read_first>
  <behavior>
    JSON-LD tests (seo-jsonld.test.ts):
    - buildScholarshipJsonLd({ title, provider_organization, host_country, ... }) returns object with @type "Grant", @context "https://schema.org", name, provider as Organization
    - buildScholarshipJsonLd with application_deadline returns applicationDeadline as ISO string
    - buildScholarshipJsonLd with eligibility_nationalities returns eligibleRegion as array of Place objects
    - buildScholarshipJsonLd with degree_levels returns educationalLevel array
    - buildScholarshipJsonLd with award_amount_max returns amount as MonetaryAmount
    - buildScholarshipJsonLd with application_url returns potentialAction with ApplyAction
    - buildBreadcrumbJsonLd([{name, url}]) returns @type BreadcrumbList with numbered ListItem elements
    - buildFaqJsonLd([{question, answer}]) returns @type FAQPage with Question/Answer mainEntity
    - buildItemListJsonLd([{name, url, position}]) returns @type ItemList with ListItem elements and numberOfItems
    - buildOrganizationJsonLd() returns @type Organization with ScholarHub name and url

    Meta tests (seo-meta.test.ts):
    - buildCanonicalUrl("/scholarships/test") returns full URL with SITE_URL prefix
    - buildOgMeta({ title, description, image, url }) returns array of og: and twitter: meta objects
    - buildPageMeta({ title, description, canonicalPath, ogImageUrl }) returns combined meta + links arrays
    - buildPageMeta strips query params from canonical URL
  </behavior>
  <action>
    **Create web/src/lib/seo/json-ld.ts** with these exported functions:

    1. `buildScholarshipJsonLd(scholarship)` -- Per D-04 and research critical finding: use @type "Grant" (NOT "Scholarship" which does not exist in Schema.org). Accept an object with fields: title, description, provider_organization, host_country, application_deadline (number|null), eligibility_nationalities (string[]|null), award_amount_max (number|null), award_currency (string|null), application_url (string|null), slug (string|null), _id (string), degree_levels (string[]), fields_of_study (string[]|null), last_verified (number|null), funding_type (string). Build JSON-LD with:
       - @context: "https://schema.org"
       - @type: "Grant"
       - name: title
       - description (if present)
       - url: SITE_URL + "/scholarships/" + (slug || _id)
       - provider: { @type: Organization, name: provider_organization }
       - funder: { @type: Organization, name: provider_organization }
       - applicationDeadline: ISO string from timestamp (if present)
       - areaServed: { @type: Place, name: getCountryName(host_country) } (if present)
       - eligibleRegion: array of { @type: Place, name } from nationalities (if present)
       - educationalLevel: degree_levels array (if non-empty)
       - amount: { @type: MonetaryAmount, value, currency } (if award_amount_max present)
       - potentialAction: { @type: ApplyAction, target: application_url } (if present)
       - dateModified: ISO string from last_verified (if present)

    2. `buildBreadcrumbJsonLd(items: {name: string, url: string}[])` -- Per D-04. Returns @type BreadcrumbList with itemListElement array of ListItem with position (1-based), name, item (url).

    3. `buildFaqJsonLd(questions: {question: string, answer: string}[])` -- Per D-03. Returns @type FAQPage with mainEntity array of Question objects, each with acceptedAnswer Answer.

    4. `buildItemListJsonLd(items: {name: string, url: string, position: number}[])` -- Per D-02. Returns @type ItemList with numberOfItems and itemListElement.

    5. `buildOrganizationJsonLd()` -- Per D-01. Returns @type Organization with name "ScholarHub", url SITE_URL, description about scholarship aggregation.

    Use a module-level `SITE_URL` constant: `const SITE_URL = typeof window !== "undefined" ? window.location.origin : (process.env.VITE_SITE_URL ?? "https://scholarhub.io")`.

    Import `getCountryName` from `@/lib/countries`.

    **Create web/src/lib/seo/meta.ts** with these exported functions:

    1. `buildCanonicalUrl(path: string): string` -- Per D-10. Returns SITE_URL + path (strips query params if any). Used in rel=canonical.

    2. `buildOgMeta(opts: { title: string, description: string, image?: string, url: string, type?: string }): object[]` -- Per D-11. Returns array of meta objects: og:title, og:description, og:image (defaults to SITE_URL + "/api/og?type=default"), og:url, og:type (defaults to "website"), twitter:card ("summary_large_image"), twitter:title, twitter:description, twitter:image.

    3. `buildPageMeta(opts: { title: string, description: string, canonicalPath: string, ogImageUrl?: string }): { meta: object[], links: object[] }` -- Combines: title meta, description meta, OG meta via buildOgMeta, canonical link via buildCanonicalUrl, hreflang="en" link per D-19.

    **Create web/src/lib/seo/landing-content.ts** with these exported functions:

    1. `generateCountryIntro(stats: { total: number, fullyFunded: number, degreeLevels: string[], topFields: string[] }, countryName: string, year: number): string` -- Per D-05/D-15. Template: "Discover {total} scholarships in {countryName} for {year}. {fullyFunded} are fully funded, covering {degreeLevels joined} programs. Popular fields include {topFields joined}."

    2. `generateCountryFaq(stats: { total: number, fullyFunded: number, degreeLevels: string[], closingSoon: number }, countryName: string): {question: string, answer: string}[]` -- Per D-03. Generate 3-5 questions: "How many scholarships are available in {country}?", "Are there fully funded scholarships in {country}?", "What degree levels have scholarships in {country}?", "Are there scholarships closing soon in {country}?", "How do I apply for scholarships in {country}?". Each answer uses real stats.

    3. `generateDegreeIntro(stats: { total: number, fullyFunded: number, topCountries: string[] }, degreeName: string, year: number): string` -- Template for degree pages.

    4. `generateDegreeFaq(stats: { total: number, fullyFunded: number, topCountries: string[] }, degreeName: string): {question: string, answer: string}[]` -- 3-5 data-driven FAQ questions for degree pages.

    5. `generateCountryCrossLinks(currentCountry: string, topCountries: string[], relatedDegrees: string[]): { countries: {name: string, slug: string}[], degrees: {name: string, slug: string}[] }` -- Per D-06. Returns arrays of related countries (top 5 excluding current) and degree levels for cross-link sections.

    6. `generateDegreeCrossLinks(currentDegree: string, topCountries: string[], allDegrees: string[]): { countries: {name: string, slug: string}[], degrees: {name: string, slug: string}[] }` -- Per D-06. Cross-links for degree pages.

    7. `generateMetaDescription(type: "country" | "degree", name: string, stats: { total: number, fullyFunded: number, degreeLevels?: string[] }): string` -- Per D-16. Template: "Discover {total} scholarships in {name} for {year}. {fullyFunded} fully funded options available for {degreeLevels}."

    **Create web/src/lib/seo/sitemap.ts** with:

    1. `generateSitemapXml(urls: { loc: string, lastmod?: string, changefreq?: string, priority?: number }[]): string` -- Per D-08. Returns valid sitemap XML string with proper XML escaping. Uses template literals (no library needed per research).

    2. `escapeXml(str: string): string` -- Helper to escape &, <, >, ", ' in URLs.

    **Create test files:**

    `web/src/__tests__/seo-jsonld.test.ts` -- Tests for all 5 JSON-LD builder functions. Mock getCountryName to return predictable values. Test that @type is "Grant" (not "Scholarship"), test optional fields are only included when present, test arrays are properly structured.

    `web/src/__tests__/seo-meta.test.ts` -- Tests for buildCanonicalUrl, buildOgMeta, buildPageMeta. Test URL construction, query param stripping, all expected meta keys present.
  </action>
  <verify>
    <automated>cd /home/siam/Personal/ScholarHub/web && npx vitest run src/__tests__/seo-jsonld.test.ts src/__tests__/seo-meta.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "@type.*Grant" web/src/lib/seo/json-ld.ts returns at least 1 (uses Grant, not Scholarship)
    - grep -c "buildScholarshipJsonLd" web/src/lib/seo/json-ld.ts returns at least 1
    - grep -c "buildBreadcrumbJsonLd" web/src/lib/seo/json-ld.ts returns at least 1
    - grep -c "buildFaqJsonLd" web/src/lib/seo/json-ld.ts returns at least 1
    - grep -c "buildItemListJsonLd" web/src/lib/seo/json-ld.ts returns at least 1
    - grep -c "buildOrganizationJsonLd" web/src/lib/seo/json-ld.ts returns at least 1
    - grep -c "buildCanonicalUrl" web/src/lib/seo/meta.ts returns at least 1
    - grep -c "buildOgMeta" web/src/lib/seo/meta.ts returns at least 1
    - grep -c "buildPageMeta" web/src/lib/seo/meta.ts returns at least 1
    - grep -c "generateCountryIntro" web/src/lib/seo/landing-content.ts returns at least 1
    - grep -c "generateCountryFaq" web/src/lib/seo/landing-content.ts returns at least 1
    - grep -c "generateDegreeIntro" web/src/lib/seo/landing-content.ts returns at least 1
    - grep -c "generateDegreeFaq" web/src/lib/seo/landing-content.ts returns at least 1
    - grep -c "generateMetaDescription" web/src/lib/seo/landing-content.ts returns at least 1
    - grep -c "generateSitemapXml" web/src/lib/seo/sitemap.ts returns at least 1
    - Test files exist and all tests pass
  </acceptance_criteria>
  <done>All 5 JSON-LD builder functions, 3 meta helpers, 7 landing content generators, and sitemap utility are implemented. Tests for JSON-LD and meta pass green. No function uses @type "Scholarship" -- all use "Grant".</done>
</task>

<task type="auto">
  <name>Task 2: Convex SEO queries and root layout SEO enhancements</name>
  <files>
    web/convex/seo.ts
    web/src/routes/__root.tsx
  </files>
  <read_first>
    web/convex/directory.ts
    web/convex/schema.ts
    web/convex/collections.ts
    web/src/routes/__root.tsx
    web/src/lib/seo/json-ld.ts
    web/src/lib/seo/meta.ts
    .planning/phases/09-seo-growth/09-RESEARCH.md
  </read_first>
  <action>
    **Create web/convex/seo.ts** with these exported queries:

    1. `getAllPublishedSlugs` -- Query returns array of { slug: string, lastModified: number } for all scholarships where status === "published". Use index "by_status" with .eq("status", "published"). Map to slug + (_updatedTime or last_verified). Used by sitemap generation.

    2. `getCountryStats` -- Args: { countryCode: v.string() }. Query scholarships by status "published" via "by_status" index, then filter by host_country === countryCode (use .filter() since there is no compound country index). Return: { total, fullyFunded (funding_type === "fully_funded" count), degreeLevels (unique degree_levels flattened), topFields (top 5 fields_of_study by frequency), closingSoon (deadline within 30 days count) }. Use .collect() then reduce in JS.

    3. `getDegreeStats` -- Args: { degreeLevel: v.string() }. Similar pattern: query published scholarships, filter by degree_levels.includes(degreeLevel). Return: { total, fullyFunded, topCountries (top 5 host_country by frequency), topFields (top 5 fields_of_study by frequency) }.

    4. `getTopCountries` -- No args. Query all published scholarships, group by host_country, return top 20 countries by count sorted descending. Each entry: { code: string, count: number }. Per D-07.

    5. `getAllDegrees` -- No args. Query all published scholarships, collect all unique degree_levels, return with counts. Each entry: { level: string, count: number }.

    All queries import from "./_generated/server" and use `v` from "convex/values". These are public queries (not internal) since they serve the public frontend.

    **Enhance web/src/routes/__root.tsx** with:

    1. **Organization JSON-LD** (per D-01): Import buildOrganizationJsonLd from "@/lib/seo/json-ld". Add a `<script type="application/ld+json">` tag in the RootComponent body (inside `<body>`, before `<CompareProvider>`) with the Organization JSON-LD.

    2. **Default OG meta** in head(): Add to the existing meta array:
       - { property: "og:title", content: "ScholarHub - Find Your Scholarship" }
       - { property: "og:description", content: "Browse 2,400+ international scholarships..." }
       - { property: "og:image", content: "/api/og?type=default" }
       - { property: "og:type", content: "website" }
       - { name: "twitter:card", content: "summary_large_image" }

    3. **hreflang** in links (per D-19): Add { rel: "alternate", hreflang: "en", href: "https://scholarhub.io" }

    4. **Robots.txt meta** (per D-09): Add to links: { rel: "robots", href: "/robots.txt" } -- actually, robots.txt is discovered automatically by crawlers at /robots.txt, so skip this. Instead, add a `<meta name="robots" content="index, follow">` to the meta array.

    5. **GA4 script** (per D-21): Add a ScriptOnce block AFTER the dark-mode ScriptOnce that injects the Google Analytics 4 gtag.js script. Use import.meta.env.VITE_GA4_ID (or process.env.VITE_GA4_ID for SSR). Only inject if the env var is truthy. Template:
       ```
       if (typeof window !== 'undefined' && '{GA4_ID}') {
         var s = document.createElement('script');
         s.src = 'https://www.googletagmanager.com/gtag/js?id={GA4_ID}';
         s.async = true;
         document.head.appendChild(s);
         window.dataLayer = window.dataLayer || [];
         function gtag(){dataLayer.push(arguments);}
         gtag('js', new Date());
         gtag('config', '{GA4_ID}');
       }
       ```

    6. **Google Search Console verification** (per D-20): Add to meta array: { name: "google-site-verification", content: import.meta.env.VITE_GSC_VERIFICATION || "" }. Only include if env var is set (conditionally add to array).

    Keep all existing head() content. The new items are additive.
  </action>
  <verify>
    <automated>cd /home/siam/Personal/ScholarHub/web && npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - File web/convex/seo.ts exists
    - grep -c "getAllPublishedSlugs" web/convex/seo.ts returns at least 1
    - grep -c "getCountryStats" web/convex/seo.ts returns at least 1
    - grep -c "getDegreeStats" web/convex/seo.ts returns at least 1
    - grep -c "getTopCountries" web/convex/seo.ts returns at least 1
    - grep -c "getAllDegrees" web/convex/seo.ts returns at least 1
    - grep -c "buildOrganizationJsonLd" web/src/routes/__root.tsx returns at least 1
    - grep -c "og:title" web/src/routes/__root.tsx returns at least 1
    - grep -c "twitter:card" web/src/routes/__root.tsx returns at least 1
    - grep -c "hreflang" web/src/routes/__root.tsx returns at least 1
    - grep -c "GA4\|gtag\|googletagmanager" web/src/routes/__root.tsx returns at least 1
    - grep -c "google-site-verification" web/src/routes/__root.tsx returns at least 1
    - All existing tests still pass
  </acceptance_criteria>
  <done>Convex seo.ts has 5 public queries for SEO data. Root layout includes Organization JSON-LD, default OG/Twitter meta, hreflang, GA4 script injection (env-var gated), and GSC verification meta tag. All existing tests pass.</done>
</task>

</tasks>

<verification>
1. `cd web && npx vitest run --reporter=verbose` -- all tests pass
2. `grep -r "@type.*Scholarship" web/src/lib/seo/` returns 0 matches (no usage of non-existent type)
3. `grep -c "Grant" web/src/lib/seo/json-ld.ts` returns at least 1
4. All 5 JSON-LD builder functions exported from json-ld.ts
5. All 3 meta helper functions exported from meta.ts
6. All 7 landing content functions exported from landing-content.ts
7. Sitemap XML generator exported from sitemap.ts
8. 5 Convex queries exported from seo.ts
9. Root layout has Organization JSON-LD, OG defaults, hreflang, GA4, GSC
</verification>

<success_criteria>
- SEO library fully created with JSON-LD, meta, landing content, and sitemap modules
- All builder functions tested and passing
- Convex SEO queries ready for consumption by routes and server routes
- Root layout enhanced with global SEO infrastructure
- No usage of non-existent Schema.org "Scholarship" type anywhere in new code
</success_criteria>

<output>
After completion, create `.planning/phases/09-seo-growth/09-01-SUMMARY.md`
</output>
